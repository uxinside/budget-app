# 변경 이력

버전은 **시맨틱 `MAJOR.MINOR.PATCH`** 를 씁니다.

| 자리 | 올리는 때 | 예 |
|---|---|---|
| **MAJOR** | 데이터·서버 계약이 바뀌어 되돌리기 어려운 변경 | 시트 컬럼 추가/삭제, API 응답 형식 변경 |
| **MINOR** | 새 기능 | 주간 예산, 카드 결제 표시 |
| **PATCH** | 버그·문구·스타일 | 칩 높이 수정, 오타 |

올릴 때는 **반드시 `python3 bump.py <버전> "<설명>" [--gas <번호>]`** 를 씁니다.
`app.js`(APP_V) · `index.html` · `sw.js` 의 번호를 한 번에 맞추고 이 파일에 줄을 추가합니다.
손으로 하나씩 고치다가 `app.css` 를 빼먹어 옛 스타일이 그대로 붙은 적이 있습니다.

Apps Script 배포 번호는 **구글이 자동으로 매기고 우리가 못 정합니다.** 그래서 여기에
앱 버전과 짝을 적어둡니다. 문제가 생겼을 때 어느 쪽을 어디로 되돌려야 하는지 보려는 것입니다.

---

<!-- 새 항목은 이 줄 아래에 -->

## 1.34.1 — 2026-08-10

- 앱 `1.34.1` · Apps Script `변경 없음`

**토큰 정리 — 화면은 그대로, 나중에 다크를 넣을 수 있게 바닥을 폅니다.**

폴 결정 2026-08-09: **테마 고르기(스타일·밝기)는 지금 안 만듭니다.** 리뉴얼이
아직 절반이라(설정·리포트·입력·시트가 남음) 지금 넣으면 남은 화면마다 테마 값을
두 번씩 맞춰야 합니다. 대신 **다크가 `:root` 한 블록 교체로 되도록** 바닥만 폅니다.

- **죽은 CSS 70규칙(4.8KB)을 걷었습니다.** 1.30~1.34 에서 지운 부품들의 스타일이
  그대로 남아 있었습니다 — `.dpn` · `.dbar` · `.dlg` · `.dsec` · `.fxrow` · `.ddtl` ·
  `.duesp` · `.hmore` · `.cfold` · `.brg` · `.two` · `.hlg` · `.mkbar` · `.drow` …
  **아무 데서도 안 그려지던 것들입니다.**
- **`#fff` 37곳을 토큰으로.** 글자는 `var(--on-ink)`, 면은 `var(--surface)`.
  스위치 손잡이 두 곳은 밝기가 바뀌어도 흰색이어야 해서 `--on-ink` 로 두었습니다.
- **⚠️ 화면은 한 픽셀도 안 바뀌었습니다.** 25장을 치환 전후로 찍어 비교했고,
  19장이 **바이트까지 동일**, 다른 6장은 **버전 문자열과 시각**뿐이었습니다.

- **`check.py` 에 「박아둔 색」 래칫을 넣었습니다** (검사 ⑥).
  본문에 토큰 안 쓰고 박아둔 색이 **기준보다 늘면 배포가 막힙니다.**
  지금 기준은 `app.css` 90곳 · `app.js` 24곳입니다.
  - 남은 90곳은 **아직 안 옮긴 화면**(설정·리포트·입력·시트)에 있습니다.
    어차피 다시 그릴 자리라 지금 고치면 두 번 일합니다 — 화면을 옮길 때마다
    줄이고 기준을 내려 적습니다. 줄었으면 검사가 알려줍니다.
  - ⚠️ JS 쪽은 `strip_noise` 대신 **주석만** 걷습니다. 그건 문자열도 지우는데,
    JS 의 색은 전부 문자열 안(`'oklch(.945 .045 '`)이라 통째로 안 보여서
    **늘 0곳이라 답했습니다.** 늘 통과하는 검사는 없는 것보다 나쁩니다.


## 1.34.0 — 2026-08-10

- 앱 `1.34.0` · Apps Script `변경 없음`

**디자인 리뉴얼 3단계 — 「앞으로 나갈 돈」이 자기 화면을 갖고, 내역 탭을 옮겼습니다.**

- **「앞으로 나갈 돈」 전용 화면(6c).** 홈·내역에 붙어 있던 접이식 패널을 걷고
  덮개 화면 하나로 모았습니다. 어두운 면에 남은 총액, 그 아래 날짜별 목록,
  가장 큰 날엔 빨강 배지. 홈과 내역엔 **금액 한 줄**만 남습니다.
  - **같은 목록이 두 곳에 있던 걸 없앴습니다.** 한쪽만 고치는 사고가 이미 한 번
    났던 자리입니다(아래 참고).
  - 디자인의 「통장 잔액 / 월말에 남는 돈」 두 줄은 **뺐습니다.** 시작 잔액이
    어디에도 없어서 낼 수 없는 숫자입니다(계좌 시트에 잔액 칸이 없습니다).
    대신 그 자리에 **카드값 · 고정 지출 · 저축 · 빚 갚기** 네 줄을 넣었습니다 —
    네 줄의 합이 위 한 줄과 같아서 어긋나면 눈으로 보입니다.
  - 「등록 / ✕(이 달은 빼기)」와 **「처리됨 · 되돌리기」** 는 디자인엔 없지만
    남겼습니다. 장부에 넣는 유일한 입구이고, ✕ 를 되돌리는 유일한 길입니다.
  - 이미 나간 카드값은 위 목록에서 빼되 **「처리됨」에 「나갔어요」로 남깁니다.**
    그냥 지우면 「분명 있었는데 왜 없어졌지」가 됩니다.

- **⚠️ 실제로 어긋나 있던 숫자를 잡았습니다.** 홈의 한 줄은 서버가 준 합계
  (`fixedLeft.amt` — 이미 등록·무시한 것까지 포함)를 쓰고, 갈래별 금액은 그걸
  빼고 세고 있었습니다. 고정 지출을 하나 등록하면 그때부터 **한 줄과 갈래 합이
  달라집니다.** 캡처로 잡았고(17,000 차이), 시험(`duescrtest`)이 이제 매번
  숫자로 검산합니다. 1.32.0 에 홈을 갈아엎으면서 이 검산을 같이 잃었던 게
  원인입니다.

- **내역 탭(7a).** 지출·수입·건수 세 값을 어두운 면으로 올려 필터보다 위에 뒀고,
  카드·그림자를 걷고 1px 선과 8px 띠로만 나눴습니다.
  - 필터 칩은 **고른 것만** 채웁니다. 「초기화」는 맨 뒤 빨강 글자로 옮겼습니다 —
    지우는 버튼이 고르는 버튼보다 앞에 설 이유가 없습니다.
  - **거래 금액에 부호와 색이 붙습니다.** 지출 `−` 빨강 · 수입 `+` 초록 ·
    **이체·저축·빚 갚기는 부호 없이 중립색**입니다. 내 돈이 자리를 옮긴 걸
    빨강으로 칠하면 손해로 읽힙니다.
  - 토글 이름을 **켜진 쪽 기준**으로 바꿨습니다: 「이체 포함」 → **「순수 거래」**
    (이체·저축 제외 N건). 예전엔 스위치가 켜지면 오히려 이체가 빠져서, 볼 때마다
    한 번씩 뒤집어 읽어야 했습니다.
  - 날짜 머리 옆 숫자가 **0 이면 안 적습니다.** 수입만 있는 날에 「0」이 뜨면
    바로 아래 급여 줄이 있는데도 「아무 일 없던 날」로 읽힙니다.

- **헤더.** 달 이름이 「2026년 8월」 → **「8월」** 로 줄었습니다(다른 해를 볼 때는
  해를 그대로 적습니다). 보는 대상은 「가구 전체」 → **「전체」**.

- **⚠️ 헤더에서 보는 대상 칩이 가운데로 밀려 있던 걸 고쳤습니다.**
  `.mkic+.who` 규칙이 가리기 아이콘이 **숨어 있을 때도** 걸려서, 칩이
  오른쪽 정렬을 잃고 달 이름 바로 옆에 붙어 있었습니다 — 달 이름의 일부처럼
  읽혔습니다. 인접 선택자는 숨은 형제에도 걸립니다. `check.py` 도 시험도 못 잡던
  종류라, 이제 **좌표로 재는 시험**을 넣었습니다.

- **덮개 화면은 탭 막대까지 숨깁니다.** 남기면 「탭 하나」로 읽히고 목록 끝 두 줄이
  막대 뒤에 깔립니다(전체 캡처에서 실제로 가려졌습니다).


## 1.33.0 — 2026-08-09

- 앱 `1.33.0` · Apps Script `변경 없음`

**디자인 리뉴얼 2단계 — 홈을 끝냈습니다.**

- **가리기 단추가 헤더 아이콘으로.** 화면마다 카드 위에 한 줄씩 얹던 걸 걷고,
  홈·내역·리포트의 **같은 자리·같은 크기**(28 원형)에 뒀습니다. 꺼짐은 아이콘만,
  켜짐은 남은 시간이 붙어 알약으로 늘어납니다. 글자(「금액 보기」)는 자리가 없어
  뺐습니다.
- **확인할 결제** — 노란 카드를 걷고 흰 면의 한 섹션으로. 배지는 어두운 알약,
  「내역 탭에 N건 더」는 제목 줄 오른쪽으로 올렸습니다(목록 아래 두면 그 줄이
  마지막 거래처럼 읽힙니다). 금액은 **마이너스 빨강**, 주 버튼은 어두운 면 채움 ·
  부 버튼은 아웃라인.
  ⚠️ **「무시」 버튼은 그대로 뒀습니다** — 디자인엔 [확인]/[따로 보기]뿐인데
  무시는 실제로 쓰는 기능이라 없애면 기능이 사라집니다.
- **카테고리** — 이름 앞에 카테고리 점 9×9, 막대는 5px, 초과 배지는 채운 빨강.
  색은 **점과 막대에만** 씁니다.
- **페이스** — 제목이 「페이스」 한 낱말이 됐습니다. 「누적 소비 vs 예산 페이스」는
  제목이 아니라 설명이었습니다. 부제는 「목표 N · 하루 N」.
  ⚠️ **경과일/한 달 토글은 남겼습니다** — 디자인엔 없지만 실제로 쓰는 기능입니다.
- 세그먼트 토글의 선택된 칸이 흰 알약에서 **어두운 면**으로. 보라는 누를 수 있는
  것에만 씁니다.
- 취소보류 건에는 마이너스 부호를 **안** 붙입니다 — 안 나갈 돈이라 「−」가 거짓말이 됩니다.

⚠️ 아직 안 옮긴 것: 내역 · 리포트 · 설정 · 입력 · 시트 8종 · 「앞으로 나갈 돈」
전용 화면(6c). 헤더의 월 표기(「2026년 8월」)와 보는 대상 칩도 디자인과 다릅니다.


## 1.32.1 — 2026-08-09

- 앱 `1.32.1` · Apps Script `변경 없음`
- **히어로 캡션(「수입의 N%」)을 디자인대로 숫자 아래·우측 정렬로** 되돌렸습니다.
  1.31.0 에 라벨 줄로 올렸던 건 **옛 배치에서 숫자가 잘렸기 때문**인데, Slate 에선
  숫자가 우측 정렬이라 그럴 이유가 없습니다. 디자인 6a 의 순서는
  **라벨 → 숫자 → 캡션 → 줄들** 입니다.
- 캡션이 없어도 **자리는 남깁니다.** 한쪽에만 캡션이 있으면 두 칸의 줄이 세로로
  어긋나서 「수입 ↔ 들어온 돈」을 나란히 못 읽습니다. 현금 흐름 쪽은 통장 기준
  전월 대비가 없어 적을 말이 없습니다 — 빈 자리로 둡니다.


## 1.32.0 — 2026-08-09

- 앱 `1.32.0` · Apps Script `변경 없음`

**클로드 디자인 리뉴얼(Slate)의 1단계** — 토큰 · 앱 셸 · 홈까지.

- **색을 셋으로 줄였습니다.** 블루 그레이(구조 · hue 262) · 보라(누를 수 있는 것) ·
  초록/빨강(부호). 카테고리 색은 배지·점·막대 같은 **작은 면에만** 씁니다.
  파랑·노랑은 구조에서 걷어냈습니다.
- **어두운 면이 화면 꼭대기에 붙습니다.** 상태바~헤더~히어로가 한 덩어리입니다.
  흰 면 가운데 띄우면 잘라 붙인 띠처럼 보입니다.
- **카드·그림자를 걷어내고 8px 띠 + 1px 선으로 구획**합니다.
- **홈 히어로가 어두운 면 안으로.** 큰 숫자는 Archivo 25px 우측 정렬,
  줄 숫자는 Space Grotesk 그대로 — 검토를 거쳐 굳힌 조합입니다.
- **「자세히」 접기가 없어졌습니다.** 근거 줄이 어두운 면 안에 10.5px 로 들어가
  접을 만큼 길지 않습니다. `pnlOpen` 키도 같이 걷었습니다.
- **부호색** — 마이너스는 빨강 + `−`(U+2212). 다만 **이체 · 저축은 중립**입니다
  (폴 결정). 내 돈이 자리를 옮긴 것까지 빨강이면 저축이 손해로 읽힙니다.
- **사람 구분이 색이 아니라 명도 3단**이 됐습니다. 카테고리 색과 안 섞입니다.
- 페이스 3분할의 칠한 알약을 걷고 값에만 색을 실었습니다.
- 탭바 활성 표시가 알약에서 **5px 보라 점 + 보라 글자**로. FAB 은 48px 원형.
- 폰트에 **Archivo**(600)를 더했습니다 — 타이틀급 숫자 전용입니다.

⚠️ **아직 안 옮긴 것** — 내역 · 리포트 · 설정 · 입력 · 시트 8종, 그리고
「앞으로 나갈 돈」 전용 화면(6c). 옛 토큰 이름은 지우지 않고 새 값으로 다시
묶어 뒀습니다. 화면을 한 장씩 옮기는 동안 두 체계가 공존해야 하는데, 이름을
갈아치우면 그 사이가 통째로 깨집니다.

⚠️ **갈래 합 검산이 지금 어디에도 없습니다.** 「앞으로 나갈 돈」의 네 줄 합이
위 한 줄과 같은지 재던 시험은 홈에서 그 목록이 빠지면서 같이 빠졌습니다 —
6c 화면을 만들 때 **반드시 그 화면 시험으로 되살려야** 합니다.


## 1.31.0 — 2026-08-09

- 앱 `1.31.0` · Apps Script `변경 없음`

폴 2026-08-09 의 홈 정리 일곱 가지입니다.

- **「수입의 N%」 배지를 손익 칸 안으로.** 카드 맨 윗줄(「이번 달」 옆)에 있으면
  두 칸 중 어느 쪽 얘기인지 알 수 없었습니다. 숫자 **바로 윗줄**에 붙였습니다 —
  같은 줄에 두면 칸(390px 에서 141px)에 안 들어가 「+2,355,649」가 「+2,355」로
  잘렸습니다. 읽는 순서로는 여전히 숫자 바로 앞입니다.
- **손익 · 현금 흐름을 오른쪽 정렬.** 밑에 붙는 목록 금액과 같은 선에 서서
  위아래로 훑어 읽힙니다.
- **두 칸을 각각의 판으로.** 가운데 실선 하나뿐이라 펼친 목록이 붙는 순간
  여덟 줄이 한 덩어리로 보였습니다. 흰 카드보다 아주 조금만 어두운 판(`--panel`)
  으로 묶었습니다 — 더 어두우면 카드가 두 장 겹친 것처럼 보입니다.
- **지출 구성 막대(고정/변동/남은 것)를 걷어냈습니다.** 1.26.0 에 되살렸던
  자리인데, 1.30.2 에서 두 칸이 각각 목록을 갖게 되면서 같은 말을 두 번 하게
  됐습니다 — 고정/변동은 막대의 색 길이보다 목록의 숫자가 정확합니다.
  CSS 도 같이 지웠습니다(죽은 이름을 남기지 않습니다).
- **「앞으로 나갈 돈」 금액에서 「원」을 뺐습니다.** 이 카드의 다른 숫자는 다 안
  적는데 여기만 붙어 있어서, 이 숫자만 다른 종류처럼 보였습니다.
- **저축과 빚 갚기를 갈랐습니다.** 저축은 내 돈이 자리를 옮기는 것이고 빚 갚기는
  남의 돈을 돌려주는 것입니다 — 둘 다 지출로는 안 잡히지만 그 뒤에 남는 게
  다릅니다. 홈의 갈래 목록과 내역 패널의 막대·범례를 **같은 기준으로** 넷으로
  나눴습니다(카드값 · 고정 지출 · 저축 · 빚 갚기).
- **페이스 카드의 「이번 주 남음」 → 「이번 달 남은 예산」.** 옛 값(`weekAllow`)은
  남은 예산 ÷ 남은 날 × 7 이라 이름은 「이번 주」인데 실제로는 오늘부터 7일이었고,
  월말이 가까워지면 크게 흔들렸습니다. 이제 **예산 − 오늘까지 쓴 돈**을 그대로
  적습니다 — 같은 카드에 그린 곡선의 마지막 점에서 내므로 그래프와 어긋날 수가
  없습니다. 예산을 넘겼으면 **음수를 그대로** 보여줍니다(0 으로 막으면 「딱
  맞췄다」로 읽힙니다). 모자랄 때만 부호를 적습니다.
- 가림 알약도 좁아진 칸에 맞춰 100×26 으로 줄였습니다.
- 시험대: `homeshot.js` 가 **큰 숫자가 칸에서 잘리는지**(`scrollWidth`) 잽니다.
  잘림은 이번에 실제로 났던 사고입니다.


## 1.30.4 — 2026-08-09

- 앱 `1.30.4` · Apps Script `변경 없음`
- **금액 가리기를 켜면 홈 레이아웃이 깨지던 것** (폴 2026-08-09: 「레이아웃이 깨져」).
  손익·현금 흐름의 가림 알약이 **178px 고정**이었는데, 1.30.1 에서 두 숫자를 두 칸으로
  나누면서 칸 폭이 148px(390px 기준) · 133px(360px)이 됐습니다. 알약이 칸을 뚫고 나가
  가운데 경계를 덮었습니다.
- 칸 안에서는 칸에 맞춘 크기를 씁니다(큰 숫자 120×30, 목록 줄 58×13). **자릿수를
  안 따라가는 고정 폭**이라는 원칙은 그대로입니다 — 실제 폭을 쓰면 자릿수가 샙니다.
- 모든 알약에 `max-width:100%` 를 걸어 **다음에 어디서 좁아지든 칸을 안 뚫게** 했습니다.
- `maskuitest` 가 이제 **360·390·430 세 폭**에서 알약과 각 줄이 칸을 넘치는지
  픽셀로 잽니다(`MW` 로 폭 지정). 여태 390 한 폭만 재고 있었습니다.


## 1.30.3 — 2026-08-09

- 앱 `1.30.3` · Apps Script `변경 없음`
- **「자세히」를 펼치면 「앞으로 나갈 돈」의 갈래도 홈에서 봅니다** — 카드값 ·
  고정 지출 · 저축 · 빚 갚기 (폴: 「펼쳤을 때 나갈 비용도 홈으로 이동하는 게
  좋을 것 같아」). 접었을 때는 예전과 똑같이 **한 줄**입니다.
- **목록과 등록·무시 단추는 여전히 내역 탭에만** 있습니다(1.28.1 의 결정).
  홈에 온 건 갈래별 금액 세 줄뿐이고, 개별 항목은 줄을 눌러 내역에서 봅니다.
- 세 줄의 합은 위 한 줄의 금액과 **정확히 같습니다.** 0 인 갈래는 줄을 안 그리고,
  안 그린 줄은 합에도 없습니다.
- ⚠️ `.ddtl` 은 `.dtl` 과 CSS 자리수가 같아 **순서로만 이깁니다.** 처음에 `.dtl`
  앞에 뒀더니 들여쓰기와 글자 크기가 조용히 무시됐습니다 — 이름 충돌로 데인
  다섯 번째 자리라, `duetest` 가 이제 실제 `padding-left`·글자 크기를 잽니다.


## 1.30.2 — 2026-08-09

- 앱 `1.30.2` · Apps Script `변경 없음`
- **펼친 근거를 두 숫자 밑, 각자의 칸 안으로.** 카드 폭을 통째로 쓰던 목록 하나를
  두 칸에 나눠 담았습니다 — 같은 내용이 여섯 줄에서 **네 줄**로 줄고, 어느 숫자의
  근거인지 화살표나 설명 없이 **자리로** 읽힙니다 (폴: 「손익액, 현금흐름 아래쪽에
  펼치면 각 금액을 표시해 주면 더 적은 높이로 채울 수 있잖아」).

  ```
  손익                  │ 현금 흐름
  +2,355,649            │ −147,915
  수입        3,693,325 │ 들어온 돈     3,693,325
  지출        1,337,676 │ 체크카드        566,100
    체크카드    566,100 │ 지난달 카드값 1,755,130
    신용카드    771,576 │ 이체 · 저축   1,520,010
  ```

- **각 칸이 그 안에서 셈이 닫힙니다.** 옆 칸을 봐야 답이 나오면 두 칸으로 나눈
  뜻이 없습니다. 그래서 수입이 양쪽에 한 번씩 섭니다.
- 오른쪽 첫 줄은 「수입」이 아니라 **「들어온 돈」**입니다. 손익 수입과 통장 수입은
  다를 수 있습니다(현금 수입은 통장에 안 들어오고, 차입·투자회수는 통장에 들어오지만
  손익 수입이 아닙니다). **같은 이름에 다른 숫자**를 적으면 그 자리에서 믿음이 깨집니다.
  → 그래서 1.29.0 의 조정 줄(「통장에 더 들어온 돈」)은 없앴습니다. 차이는 이제
  두 줄의 숫자로 그냥 보입니다.
- 「들어온 돈」도 수입이라 **가리기 대상**입니다. 안 가리면 왼쪽을 가린 게 소용없습니다.
- 목록이 둘로 갈렸으므로 누르는 것도 **카드 한 곳에서 위임**으로 받습니다.
  `#dtl` 하나만 잡으면 오른쪽 칸은 눌러도 아무 일이 없습니다.
- 좁은 칸(360px)에서 「지난달 카드값」이 두 줄로 접혀 양쪽 줄이 어긋났습니다.
  칸 안에서만 글자를 11px/11.5px 로 줄여 **360·390·430 세 폭 모두 네 줄**로 맞췄습니다.
- 시험대: `homeshot.js` 에 **폭(`HW`)과 펼침(`HOPEN`)** 을 넣어 세 폭을 다 찍고,
  각 줄이 칸을 넘치는지(`scrollWidth`) 같이 잽니다.

## 1.30.1 — 2026-08-09

- 앱 `1.30.1` · Apps Script `변경 없음`
- 홈 정리 — 손익·현금 흐름 두 숫자 먼저, 앞으로 나갈 돈, 근거는 펼쳐서

### 폴: 「깔끔하지 않네」

1.30.0 은 계산서를 통째로 펼쳐 놓고 갈래 상자 둘로 갈랐습니다. 산술은 맞았지만
**매일 보는 건 두 숫자**고, 그게 어떻게 나왔나는 궁금할 때만 봅니다.

```
이번 달                        [수입의 36%]
손익            │ 현금 흐름
+2,355,649      │  −147,915
▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬
🗂 앞으로 나갈 돈 · 11건        3,297,268원 ›
──────────── 자세히 ⌄ ────────────
   수입                        3,693,325
   지출                        1,337,676
      체크카드                   566,100
      신용카드                   771,576
   지난달 카드값                1,755,130
   이체 · 저축                  1,520,010
```

### ⚠️ 들여쓰기가 계산을 대신합니다

지출 아래 체크·신용이 **한 칸 들어가 있어서** 「지출 = 체크 + 신용」이 글자 없이
읽힙니다. 그래서 1.30.0 의 소계 줄(「여기까지는 같습니다」)도, 갈래 상자도 필요
없어졌습니다 — 그 둘이 하던 일을 여백이 합니다.

```
손익      = 수입 − 지출
현금 흐름 = 수입 − 체크 − 지난달 카드값 − 이체·저축
```

둘 다 이 목록 안에서 나오고 **같은 숫자가 두 번 나오지 않습니다.**
`maskuitest` 가 화면 숫자로 `지출 = 체크 + 신용` 을 검산합니다 — 들여쓰기가
계산을 대신하는 구조라, 숫자가 안 맞으면 그 여백이 거짓말이 됩니다.

### 「이번 달 남은 돈」 → 「현금 흐름」

폴이 쓴 말로 돌아갑니다. **흐름이지 잔액이 아니고**, 시작 잔액이 없어서 잔액은
낼 수 없습니다. `cashtest` 가 「잔액」이 되살아나면 실패합니다.

### 그 밖에

- 기본은 **접힘**. 펼침 상태는 로컬에 저장합니다 — 수신함 하나만 확인해도 홈이
  다시 그려지는데, 그때마다 닫히면 보던 사람에겐 제멋대로 닫히는 걸로 보입니다.
- **체크·신용은 눌리지 않습니다.** 구분이 아니라 **결제수단**으로 나눈 것이라
  내역에서 거를 수가 없습니다 — 누를 수 있는 것처럼 그려두면 눌러도 아무 일이
  없는 줄이 됩니다. 나머지 줄은 눌러서 그 유형만 내역에서 봅니다.
- 「금액 가리기」는 **두 큰 숫자를 다 가립니다.** 손익만 가리면 현금 흐름 + 카드값 +
  이체로 수입이 나오고, 현금 흐름만 가리면 손익 + 지출로 나옵니다.
- 현금 흐름 자리는 내역이 오기 전에 **미리 잡아 둡니다.** 오는 순간 생기면
  아래가 통째로 밀립니다.

### 시험

`cashtest` 59 · `cashcalctest` 46 · `maskuitest` 46 · `masktest` 99 ·
`cardpaidtest` 41 을 새 구조로 옮겼습니다.

⚠️ 「여기까지는 같습니다」가 **왜 없앴는지 주석에** 남아 있어 「없어졌나」 시험이
걸렸습니다 — 「일괄」·「예상 잔액」·「못 셌어요」에 이어 **네 번째** 같은 함정입니다.
주석을 걷어낸 코드만 봅니다.


## 1.30.0 — 2026-08-09

- 앱 `1.30.0` · Apps Script `변경 없음`
- 손익과 잔액을 한 계산서로 — 수입은 한 번만, 두 갈래로 갈라 내려간다

### 폴: 「난 동어 반복을 줄이고 싶어서 그래」

> 수입은 한 번만 쓰고, 밑에 체크·신용 지출액 빼서 지출액 보여주고. 그 밑에
> 신용카드·이체 금액을 보여주면 잔액이 나오니까. 금액은 손익, 잔액만 보여주면 되는거지.

손익 카드와 「이번 달 남은 돈」 카드가 **한 카드**가 됐습니다. 수입은 화면에 한 번,
큰 숫자는 **손익과 잔액 둘**뿐입니다.

```
  수입                            3,693,325
− 체크로 쓴 돈  바로 빠짐            566,100
= 여기까지는 같습니다              3,127,225
┌ 쓴 걸로 치면
│ − 신용으로 쓴 돈  다음 달에 빠짐    771,576
└ 이번 달 손익                  +2,355,649원  [수입의 36%]
┌ 통장에서 나간 걸로 치면
│ − 지난달 카드값                 1,755,130
│ − 이체 · 저축  쓴 건 아님        1,520,010
└ 통장에 남은 것                  −147,915원
```

### ⚠️⚠️ 한 줄로 쭉 빼면 틀립니다 — 소계 줄을 지우지 마세요

말씀대로 선형으로 이어 내려가면 **손익까지는 정확히 맞는데** 그 아래가 틀립니다:

```
3,693,325 − 566,100 − 771,576 = 2,355,649   ← 손익 ✓
          − 1,755,130 − 1,520,010 = −919,491 ✗   (실제 잔액은 −147,915)
```

이미 뺀 **신용 771,576** 이 통장에선 이번 달에 안 나갔는데 계속 빠진 채로 내려가서,
마지막이 실제 잔액보다 정확히 그만큼 적습니다. **손익과 잔액은 같은 줄기에서 안 나옵니다.**

그래서 공유되는 앞부분(수입 − 체크)을 **「여기까지는 같습니다」 한 줄로 못박고**
거기서 두 갈래로 내려갑니다. 이 소계 줄이 없으면 아래 두 덩이가 이어져 보여서
사람이 잘못 더합니다 — **지우면 안 됩니다.** `cashtest` 가 지킵니다.

한 줄로 잇는 방법(손익 뒤에 `+ 신용 771,576` 을 다시 더하는 다리)도 그려서 같이
봤는데, 그건 **771,576 이 두 번** 나옵니다 — 폴이 지우려던 바로 그 반복이라 버렸습니다.
1.24.0 에 그 모양을 만들었다가 「너무 복잡하다」고 걷어낸 적도 있습니다.

### ⚠️ 손익 수입과 통장 수입은 다를 수 있습니다

`p.income`(손익)과 `f.inc`(통장)은 같은 값이 아닙니다 — 현금으로 받은 수입은
통장에 안 들어오고, **차입·투자회수**는 통장에 들어오지만 손익 수입이 아닙니다
(`CASH_DIR` 이 그 둘을 +1 로 셉니다). 수입을 한 번만 쓰기로 한 이상 그 차이를
어딘가 적어야 계산이 닫힙니다 — **통장 갈래에 한 줄**로 적습니다:

- 통장에 더 들어옴 → `+ 통장에 더 들어온 돈 · 차입·투자회수 등`
- 통장엔 덜 들어옴 → `− 통장엔 안 들어온 수입 · 현금 등`

**대개 0 이라 줄이 아예 안 생깁니다.** 0 인데 그려두면 그게 더 헷갈립니다.

### 위계는 카드가 아니라 갈래 상자 색이 만듭니다

1.24.0 접기 → 1.26.0 별도 tonal 카드 → 1.30.0 **한 카드 안의 두 갈래.**
「쓴 걸로 치면」은 mint, 「통장에서 나간 걸로 치면」은 sky. 이 두 머리말이
1.29.1 에서 지운 **「두 카드 기준이 다르다」 설명을 문장 없이** 대신합니다.

### ⚠️ 이름 충돌 — 네 번째, 이번엔 «중복 정의»

지출 구성 막대를 `.pbar` 라고 지었는데 **「사람별」 막대가 이미 그 이름을 쓰고
있었습니다** (`.pbar{height:12px;margin-top:14px}` · line 340). 뒤에 정의된
쪽이 이겨서 **사람별 막대가 계속 10px 로 그려지고 있었습니다** — 눈에 안 띄게,
아마 1.26.0 부터 쭉. `.spbar` 로 바꿨습니다.

`.fc`(1.27.0) · `.psub`(1.28.0) · 중복 `.pbar`(1.30.0). `check.py` 는
「CSS 에 없는 클래스」만 봅니다 — **이미 있는 이름은 못 잡습니다.**
`cashcalctest` 에 「`^\.pbar\{` 정의가 한 곳뿐인가」를 넣었습니다.

### ⚠️ flex 안에서 마스크 알약이 눌렸습니다

「금액 가리기」의 히어로 알약은 178px 고정인데, 옆에 라벨·배지가 있는 flex 줄에서는
**121px 로 눌려서** 그려졌습니다. 폭을 고정한 이유가 자릿수를 숨기려는 것인데
눌리면 그 고정이 무너집니다. `flex:0 0 auto` 를 붙였습니다 —
`maskuitest` 가 실측해서 잡았습니다.

### 걷어낸 것

`cardCash()` · `CASH_K` · `cashOpenGet()` · `.cashf` · `.cfh` · `.cshe` · `.hb` ·
`#h3` 핸들러. 계산서 줄 클릭은 이제 **카드 하나에 한 번만** 물립니다
(줄이 계산서·두 갈래 세 군데에 흩어져 있어서, `querySelector` 로 하나만 잡으면
나머지는 눌러도 아무 일이 없습니다).

### 시험

- `cashtest` 57건 — 한 카드 · 소계 줄 · 두 머리말 · 수입이 카드에 한 번만 ·
  두 수입 차이를 적는가 · 통장 갈래 자리 잡기.
- `cashcalctest` 49건 — 갈래 색 · 두 결론만 색 · 결론 라벨 한 줄 고정 ·
  `.pbar` 중복 없음 · `.bg` 스코프.
- `maskuitest` 46건 — 소계도 가리는가(안 그러면 수입이 역산된다) ·
  두 결론이 **각각 178×30 인가**(눌림 검출) · 체크·신용은 안 가려지는가.

⚠️ `cashtest` 에서 「수입이 한 번만 나오는가」를 파일 전체로 세니 21곳이 나왔습니다
(`CASH_DIR`·필터 목록까지 딸려 옴). **손익 카드 안**만 보게 좁혔습니다 —
세는 범위가 틀리면 시험이 아무 말도 안 하게 됩니다.


## 1.29.1 — 2026-08-09

- 앱 `1.29.1` · Apps Script `변경 없음`
- 앞으로 나갈 돈 부제를 건수만으로 · 이번 달 남은 돈 부제·각주 삭제

### 「앞으로 나갈 돈」 부제 = 건수 하나 (홈·내역 같은 값)

`카드 1,400,000 (8/25) · 고정비 10건` → **`11건`**.
큰 숫자가 바로 옆에 또 있어서 같은 말을 두 번 했습니다. 갈래별 금액과 결제일은
누르면 내역 패널에 다 있습니다.

건수는 `nextDue()` 가 한 번 세서 `due.n` 으로 내려보냅니다 — 홈과 내역이 **같은 값**을
씁니다. 두 곳에서 따로 세면 어긋납니다. 결제일은 사라진 게 아니라 `due.pay` 에
그대로 있고, 화면에서는 카드값 섹션 머리(「8월 25일 · 7월 사용분」)가 말합니다.

⚠️ `cardpaidtest` 가 결제일을 **부제 글자**(`/\(8\/25\)/`)로 재고 있었습니다.
`due.pay` **값**을 재도록 바꿨습니다 — 표시 자리가 바뀌어도 안 깨집니다.

### 「이번 달 남은 돈」 — 부제와 각주 전부 삭제

걷어낸 것: 부제 「통장에 실제로 오간 돈」, 「이번 달 카드로 쓴 N원은 다음 달에
빠져나갑니다」, 「밖으로 보낸 이체 N건」, 「내 통장끼리 옮긴 N건은 뺐어요」,
「계좌 시트에 없는 결제수단 N건은 못 셌어요」, 「자본거래 N건」.

**⚠️ 무엇을 잃었는지 적어 둡니다.**

1. 1.29.0 에서 손익 쪽 「쓴 날짜 기준」을 지웠고, 이제 부제까지 지웠습니다.
   **두 카드가 왜 다른지 화면에 적힌 설명이 하나도 없습니다.** 남은 건 펼치면
   나오는 계산서 네 줄뿐입니다 — 그래서 그 계산서는 지우면 안 됩니다.
   `cashtest` 가 네 줄이 다 있는지 지킵니다.
2. 「계좌 시트에 없는 결제수단 N건은 못 셌어요」는 **계산이 그만큼 틀렸다는 신호**
   였습니다. 지금은 조용합니다. 계좌 시트에 그 결제수단을 넣으면 애초에 안 뜨던
   문구입니다 — 데이터를 채우는 쪽이 문구를 지우는 것보다 낫습니다.

**세는 것은 그만두지 않았습니다.** `cashFlow` 는 지금도 `unknownN`·`capN`·`innerN`·
`sendN` 을 다 셉니다. 화면 문구만 뺐습니다 — 다시 보여주기로 하면 그때부터 값이
있어야 하고, 세기까지 그만두면 그 사이 값은 영영 없습니다.
`cashcalctest` 가 「그래도 계속 센다」를 지킵니다.

⚠️ **지운 문구는 「왜 지웠는지」 주석에 남습니다.** 「없어졌나」를 재는 시험은
주석을 걷어낸 코드를 봐야 합니다 — 1.27.0 「일괄」, 1.29.0 「예상 잔액」에 이어
**세 번째**로 같은 데 걸렸습니다. `cashtest`·`cashcalctest` 는 이제 파일 맨 위에서
`bare` 를 만들어 씁니다.


## 1.29.0 — 2026-08-09

- 앱 `1.29.0` · Apps Script `변경 없음`
- 홈 다이어트 — 앞으로 나갈 돈은 금액 한 줄 · 「이번 달 남은 돈」으로 이름 바꿈 · 목표 금액 미리 받기 · 못 알아본 이체를 드러냄

폴 2026-08-09 의 여섯 가지를 한 판에 담았습니다.

### ① 홈은 금액 한 줄까지

「홈이 너무 복잡해. 금액만 보여줘. 나머지는 내역으로 가서 펼쳐지도록.」

1.28.1 에 **막대·범례 세 줄**을 홈에 남겨 뒀는데, 그게 다시 홈을 길게 만들었습니다.
갈래별 금액은 누르면 내역에서 다 보입니다. 홈은 「앞으로 나갈 돈 · 3,297,268원 ›」
한 줄입니다.

⚠️ 막대가 빠지면서 `.hdue.go` 의 아래 여백(8px)이 **로딩 줄과 6px 어긋났습니다.**
자리를 미리 잡아 두는 이유가 화면이 안 튀게 하려는 것인데, 그 여백이 남아 있으면
리포트가 오는 순간 살짝 튑니다. `duetest` 의 기준을 「안 늘어난다」에서
**「높이가 아예 같다」**로 올렸습니다 — 이제 한 줄 ↔ 한 줄이라 줄어들 것도 없습니다.

### ② 「통장 흐름」 → **「이번 달 남은 돈」**

폴이 「'예상 잔액'이란 표현은 어떨까?」라고 물었습니다. **쓸 수 없습니다** —
잔액을 내려면 시작 잔액이 있어야 하는데 어디에도 없습니다. 계좌 시트엔 잔액 칸이
아예 없고, 자산 시트는 계좌 단위가 아니라 월별 스냅샷입니다. (계좌 시트에 「잔액·기준일」
칸을 신설하는 건 폴이 「나중에」로 미뤄둔 항목입니다.) 이름만 바꾸면 **없는 정확도를
파는 것**이 됩니다.

「이번 달」이 범위를 말해 주니까 잔액으로 안 읽힙니다. 계산서 맨 아랫줄이 이미
「남은 것」이라 이름과 결론이 같은 말을 합니다. `cashtest` 가 「예상 잔액」·「잔액」이
되살아나면 실패하게 지킵니다.

### ③ 페이스 카드의 목표 수정 버튼 제거

「누적 소비 vs 예산 페이스 옆 목표 수정 버튼은 지워도 돼.」

연필과 「바꿈」 배지를 걷어내고 예산 줄을 그냥 글자로 바꿨습니다. 여기서 봐야 하는 건
**지금 얼마 쓰고 있나**지 목표를 고치는 게 아닙니다.

⚠️ 다만 **입구가 0개가 되면 안 됩니다.** 카테고리 카드 헤더의 「목표 ✎」와
「이 달 목표 안 정함」 배너 둘은 그대로입니다 — `budentrytest` 가 입구 수를 셉니다.

### ④ 목표 금액을 앱 뜰 때 미리 받기

「목표 금액 불러오는 것도 느린데 앱 로딩할 때 미리 불러오도록.」

부팅 뒤 뒤에서 `budgetPlan` 을 받아 두고, 목표 화면은 **캐시로 즉시** 열린 뒤
최신을 다시 받아 덮습니다.

⚠️ 캐시로 열어 놓고 뒤늦게 온 응답으로 덮으면 **사람이 고치던 값이 날아갑니다.**
고친 게 있으면(`dirty()`) 안 덮습니다. 목표를 저장하면 캐시를 버립니다.
`buduitest` 가 서버를 900ms 느리게 해 두고 **「불러오는 중」이 안 뜨는지**를 잽니다.

### ⑤ 문구 다이어트

- **「8월 9일까지 · 쓴 날짜 기준」 삭제.** 「N일까지」는 달력을 보면 압니다.
  ⚠️ 그런데 그게 두 카드의 **기준이 다르다**는 걸 알리던 자리였습니다. 이제 그 말은
  「이번 달 남은 돈」의 부제(「통장에 실제로 오간 돈」) 한 곳이 합니다 —
  `cashtest` 가 그 부제까지 지워지면 실패합니다.
- **「수입의 36%」 배지를 큰 금액 뒤로.** 제목 옆에 있으면 「이번 달 손익 (수입의 36%)」로
  읽혀서 무엇의 36% 인지 헷갈립니다. 금액 바로 뒤가 그 숫자의 주석입니다.

### ⑥ 「자체 이체면 0인 게 맞잖아」 — 맞습니다. 다만 **알아봐야** 뺍니다

규칙은 이미 있었습니다. `내용`에 받는 계좌 이름이 있으면 내 통장끼리로 보고
**합에서 뺍니다**(「내 통장끼리 옮긴 N건은 뺐어요」). 문제는 **이름이 없으면 못
알아본다**는 것입니다 — 「전세금 송금」이 밖으로 나간 건지, 자체 이체인데 이름을
안 적은 건지 화면에서는 가릴 방법이 없습니다.

거래내역 시트에는 **보내는 계좌 한 칸**만 있습니다(E열). 받는 쪽 칸이 없으니
서버도 모릅니다. 그래서 **가리는 척하지 않습니다** — 나간 것으로 세되(과소평가가
더 나쁩니다) 건수·금액을 적고 **고치는 법까지** 씁니다:

> 밖으로 보낸 이체 1건(1,500,010)은 나간 것으로 셌어요 · 내 통장끼리 옮긴 거면
> 내용에 받는 계좌 이름을 넣어주세요

그리고 「이체 · 저축」 줄을 **누를 수 있게** 했습니다 — 내역에서 그 건들을 바로 봅니다.
⚠️ 이 줄은 이체 + 저축/투자 + 부채상환 **셋을 합친 것**이라 하나만 걸면 반쪽만 보입니다.
`data-g` 를 `|` 로 나눠 세 구분을 한 번에 겁니다.

### 시험

- `cashcalctest` 에 자체 이체 8건 추가 — 이름을 적은 건 0 이 되고, 못 알아본 건은
  세어지되 **건수가 남는지**까지 잽니다.
- `buduitest` 에 미리 받기 3건. `budentrytest` 는 입구 개수를 셉니다.
- `duetest`·`duepaneltest`·`cardpaidtest`·`duesplittest` 를 홈 한 줄에 맞춰 옮겼습니다.

⚠️ `mkbudprev` 가 `showBudget` **함수만** 떼어 오고 있었는데, 그 함수가 모듈 밖
`budCache` 를 읽게 되면서 미리보기가 통째로 안 그려졌습니다. 함수를 떼어 올 땐
**그 함수가 읽는 상태도 같이** 가져와야 합니다 — 1.28.0 의 `PIN_K` 와 같은 실수입니다.

⚠️ 「예상 잔액」은 **주석에도** 나옵니다(쓰지 말라고 적어 둔 것). 문구 시험은
주석을 걷어낸 코드만 봅니다 — 1.27.0 의 「일괄」과 같은 함정입니다.


## 1.28.1 — 2026-08-09

- 앱 `1.28.1` · Apps Script `변경 없음`
- 고정 지출 목록·등록을 다시 내역 탭으로 (홈은 요약까지) · 통장 흐름이 마이너스면 접혀 있어도 붉게

### 폴: 「고정 지출 수정을 홈으로 옮기려는 의도는 아니었어」

1.27.0 에서 「앞으로 나갈 돈」을 통째로 홈 hero 안에 넣었습니다. 디자인 핸드오프의
앵커가 그랬고 저도 「탭을 안 옮기는 게 낫다」고 읽었는데, **폴이 원한 건 목록이
홈으로 오는 게 아니었습니다.**

**보여주는 것과 손대는 것은 다른 일입니다.** 얼마나 나갈지는 매일 보는 숫자라
홈에 두고, 등록·무시처럼 **장부를 바꾸는 일**은 내역 탭 한 곳에서만 합니다.

| | 홈 | 내역 |
|---|---|---|
| 1.27~1.28.0 | 한 줄 → 눌러서 **그 자리에 펼침** (목록·등록 전부) | — |
| **1.28.1** | 한 줄 + **3분할 막대·범례**까지. 누르면 내역으로 | 카드값·고정 지출 **목록과 등록** |

- 홈의 화살표를 `⌄`(펼침) 에서 `›`(이동) 으로 바꿨습니다 — 누르면 무엇이 일어나는지가
  달라졌으니 표시도 달라져야 합니다.
- 막대를 눌러도 같은 곳으로 갑니다. 「고정 지출 7건이 뭐지」가 그 숫자를 보다 드는
  질문이고, 답이 거기 있습니다.
- **넘어갈 때 패널을 펴 둡니다.** 보러 갔는데 접혀 있으면 한 번 더 눌러야 합니다.
- 패널이 내역에만 있으니 거기서는 **펼침이 기본**입니다(1.27.0 은 접힘이 기본이었습니다).

⚠️ 내역 탭에는 목록 전체를 받는 위임 클릭(`#screen`)이 이미 있습니다. 패널 클릭에
`stopPropagation()` 을 안 걸면 **[등록]을 눌렀을 때 고치기 화면이 같이 뜹니다.**
`duetest` 가 「패널을 눌러도 고치기 화면이 안 뜬다」로 잽니다.

⚠️ 홈 로딩 스켈레톤도 막대·범례 세 줄까지 **최대치로** 자리를 잡습니다. 안 그러면
리포트가 오는 순간 hero 가 자라서 아래가 통째로 밀립니다(폴 지적 2026-08-06).
`duetest` 실측 386px → 356px — 늘지 않고 30px 줄어듭니다.

### 통장 흐름이 마이너스면 접혀 있어도 붉게

펼치면 「남은 것 −147,915」가 빨간데, **접으면 머리의 같은 숫자가 검정**이었습니다.
접어 두고 쓰시는 분은 마이너스인 걸 못 봅니다. 같은 값이 상태에 따라 다른 색이면
그건 색이 뜻을 잃은 것입니다. 펼쳤을 때와 **같은 규칙**을 씁니다 —
플러스는 mint, 마이너스는 coral.

### 시험

- `duetest` 22건 · `duepaneltest` 42건 · `staletest` 12건으로 **자리를 따라 옮겼습니다.**
  「홈에 등록 버튼이 없다」와 「내역에 등록 버튼이 있다」를 **양쪽 다** 잽니다 —
  한쪽만 재면 두 곳에 남아도 통과합니다.
- `cashcalctest` 에 접힌 머리 색 3건 추가.

⚠️ `duetest` 에서 범례를 `innerText` 로 읽고 줄바꿈으로 잘랐는데, 보조어(`<em>`)가
**인라인**이라 줄바꿈이 없어 통째로 딸려 왔습니다. `firstChild.textContent` 로 고쳤습니다.


## 1.28.0 — 2026-08-08

- 앱 `1.28.0` · Apps Script `변경 없음`
- 리포트 금액 가리기 — 전체화면 잠금을 걷어내고 금액만 알약으로 (디자인 핸드오프 PART 3 · 11a~11d)

### 1.19.0 의 전체화면 PIN 잠금을 폐기합니다

페이지를 통째로 덮으면 **어떤 항목이 있는지조차 못 봅니다.** 옆 사람 눈을 막자고
주인까지 못 보게 만든 셈이었습니다. 게다가 잠금이 켜져 있으면 리포트를 캐시에
안 남겨서 탭을 열 때마다 처음부터 받아야 했습니다.

| 1.19.0~1.27.0 | 1.28.0 (디자인) |
|---|---|
| 리포트 **전체를 덮는** 잠금 화면 | 구조·라벨·비율·그래프는 그대로, **금액만** |
| 탭바까지 걷어냄 (다른 탭으로 못 감) | 앱은 평소대로 — 숫자만 알약 |
| 볼 때도 잠글 때도 PIN | **가릴 땐 인증 없이**, 볼 때만 PIN |
| 5번 틀리면 **로그아웃** | 30초 쉬기 |
| PIN 켜지면 리포트 캐시 안 남김 | 캐시 그대로 (표시 설정이라 버릴 이유가 없음) |

**이건 표시 설정이지 접근 제어가 아닙니다.** 폰을 남이 들고 앱을 열면 가릴 곳 밖의
숫자는 그대로 보입니다. 막으려는 건 「어깨 너머 시선」입니다 — 그 목적이라면
전체를 덮을 이유도, 캐시를 버릴 이유도 없습니다.

### 마스크

- **점(●●●)도 블러도 아닌 고정 폭 회색 알약.** 점은 자릿수를 세게 만들고,
  블러는 큰 숫자의 윤곽이 남습니다.
- 폭은 **자릿수와 무관하게** 히어로 178×30 · 중간 86×15 · 목록 78×13 ·
  문장 속 62×12 · 축 라벨 44×9. `maskuitest` 가 자릿수가 다른 여섯 금액을
  실제로 재서 **전부 78px 인지** 확인합니다.
- 로딩 스켈레톤과 색이 겹치지 않게 — 스켈레톤은 빛이 흐르고 마스크는 가만히 있습니다.

**남기는 것**: 카드 제목 · 항목 이름 · **비율(%)** · 추이 곡선과 점 · 가로축 달 이름 ·
진단 문장 · 개월 수. 가리는 게 아니라 지워버리면 실패입니다.

### ⚠️ 「홈은 수입만」을 글자 그대로 하면 옆 숫자로 역산됩니다

디자인은 홈에서 **수입 금액만** 가리고 지출은 남기라고 합니다. 그런데 홈 히어로에
**손익**이 있습니다 — 손익 + 지출 = 수입이라, 수입만 가리면 바로 위 숫자에서
그대로 나옵니다. 통장 흐름 계산서도 같습니다(남은 것 + 나간 것 = 들어온 돈).

그래서 **손익과 「남은 것」도 같이 가립니다.** 지출은 디자인대로 그대로 둡니다 —
완료 기준 「홈·내역에서 지출 금액은 가려지지 않는다」는 지킵니다.
가리는 목적이 「수입이 안 읽히는 것」이라면 이게 그 목적입니다.

### 다시 그리지 않습니다

마크업에는 「가릴 수 있는 자리」(`.mk`)만 붙고, 실제로 가릴지는 **`body.mkon`
클래스 하나**가 정합니다. 그래서 풀고 잠글 때 `render()` 를 안 부릅니다 —
다시 그리면 숫자가 튀어나오고 **스크롤 자리를 잃습니다.**
`maskuitest` 가 DOM 노드에 표시를 남겨 두고 푼 뒤에 **그 노드가 그대로인지**로
잽니다. 덤으로 120ms cross-fade 가 공짜입니다.

### 옛 PIN 을 조용히 버리지 않습니다

「리포트 잠금」을 켜 두셨던 분은 **이미 「가리고 싶다」고 말한 것**입니다. 설정이
한 번도 저장된 적 없는데 PIN 만 있으면 **켜진 것으로 잇습니다** — 안 그러면 앱을
올리는 순간 자산이 통째로 드러납니다. 한 번이라도 직접 끄셨으면 그 결정이 이깁니다.

**PIN 이 없으면 가리기 자체가 안 켜집니다.** 풀 방법이 없는 가림은 기능이 아니라 고장입니다.

### 설정 · PIN 시트

- 설정 **「표시」** 묶음(보안 아님 — 잠금 화면 설정이 아니라 표시 설정입니다).
- 가릴 곳: 리포트 전체 금액 / 홈 수입만 / 내역 수입 거래만. **세 곳을 다 끄지는
  못합니다** — 켜 둔 의미가 없어집니다.
- 다시 가리기: 앱 나가면 / 10분 뒤 / 직접 끌 때. 「앱 나가면」은 **나갈 때** 판정합니다 —
  돌아올 때 재면 그 사이 앱 전환 화면에 금액이 그대로 남습니다.
- PIN 시트는 **전체를 안 덮는 바텀시트**입니다. 뒤 화면이 보여야 무엇을 열려는지 압니다.
  좌하단은 빈 칸 — 생체 인증은 안 씁니다. 취소 버튼도 없습니다(밖을 누르면 취소).

### ⚠️ 또 클래스 이름 충돌 — 이번엔 `.psub`

PIN 시트의 설명 줄을 `.psub` 라고 지었는데 **페이스 차트가 이미 쓰고 있었습니다**
(`display:flex`, 스코프 없음). 1.27.0 의 `.fc` 와 똑같은 종류입니다 —
`check.py` 는 「CSS 에 없는 클래스」는 잡아도 **이미 있는 이름**은 못 잡습니다.
`.pdesc` 로 바꾸고, `masktest` 에 「스코프 없이 정의된 같은 이름이 있는가」를,
`maskuitest` 에 「설명 줄의 `display` 가 정말 block 인가」를 넣었습니다.

### 시험

- `masktest.js` 98건 신규 — 옛 잠금 제거 · PIN 없으면 안 켜짐 · 다시 안 그림 ·
  알약 5크기 · 역산 방지 · 지출은 안 가림 · 이름 충돌.
- `maskuitest.js` 44건 신규 — **브라우저에서 실제로 잽니다.** 알약 실측 크기,
  자릿수가 달라도 같은 폭, %·곡선·이름이 남는지, PIN 시트가 뒤를 안 덮는지,
  틀린 PIN → 안 풀림, 맞는 PIN → 같은 DOM 노드 유지.
- `healthtest.js` 는 진단 문장이 자리표(`⟪⟫`)를 달고 나오게 바뀌어, 문구를
  잴 때 자리표를 걷어내는 `T()` 를 씁니다. **자리표까지 정규식에 박아 두면
  마스크 방식을 바꿀 때마다 문구 시험이 같이 깨집니다.**

⚠️ `masktest` 를 만들다 vm 샌드박스에 상수(`PIN_K`)를 안 넣어서 `try/catch` 가
`ReferenceError` 를 삼켰고, **조용히 「PIN 없음」으로 읽혀** 시험이 통과할 뻔했습니다.
떼어 올 때 상수도 같이 가져오세요.


## 1.27.0 — 2026-08-08

- 앱 `1.27.0` · Apps Script `변경 없음`
- 앞으로 나갈 돈 — 홈에서 그 자리에 펼치는 한 덩어리 (디자인 핸드오프 PART 2 · 앵커 10c)

### 오늘 오후에 한 판단을 되돌립니다

1.25.0 에서 세 갈래(카드값 / 고정 지출 / 저축·빚)를 **내역 탭의 별도 카드**로
옮겼습니다. 그때 근거는 「매일 보는 게 아니라 궁금할 때 보는 것」이었고, 그건
**한 줄로 안 접히던 상태**에서는 맞는 말이었습니다. 접히고 나면 얘기가 달라집니다 —
홈에 있어도 평소엔 한 줄이고, 볼 때 탭을 옮길 필요가 없습니다.

| 1.25.0 | 1.27.0 (디자인) |
|---|---|
| 홈은 한 줄, 누르면 **내역 탭으로 이동** | 홈에서 **그 자리에 펼침** |
| 요약·카드값·고정비가 각각 따로 | 셋이 **하나의 collapse 단위** |
| 같은 목록이 홈·내역 두 곳 | **홈 한 곳** |

두 곳에 남는 게 이 변경의 진짜 위험이었습니다. 한쪽만 고치는 사고가 나기 때문에,
옛 부품(`cardDueAll` · `dueCardsHtml` · `dueFixHtml` · `dueSplitHtml`)을 **지웠고**
시험도 「내역 탭에 없다」를 직접 잽니다.

### 이번에 새로 잡은 것

- **3분할 막대 + 범례** — 카드값(지출은 이미 반영됨) / 고정 지출(N건) / 저축·빚(지출 아님).
  0 인 갈래는 세그먼트도 범례도 안 그려 2분할이 됩니다. 폭 합은 항상 100%.
- **고정 지출 행이 한 줄, 42px 고정** (디자인 QA 1번). 이름만 ellipsis 로 줄고
  결제수단은 안 줄어듭니다. 긴 이름에서 줄바꿈이 나면 목록이 통째로 들쭉날쭉해집니다.
- **날짜는 그룹 머리에 한 번만.** 행마다 반복하지 않습니다.
- **처리된 건은 아래 「처리됨」 접이로** 내려갑니다 — 남은 것만 위에 보입니다.
- **지난 달을 보고 있으면 블록 전체를 숨깁니다.** 「앞으로」가 아닙니다.
- 일괄 등록 버튼은 **없습니다.** 등록은 건별로만 — 한 번에 여러 건이 장부에 들어가면
  뭐가 들어갔는지 못 보고 지나갑니다.

### ⚠️ 클래스 이름 충돌 (check.py 가 못 잡는 종류)

막대 세그먼트를 `cd`/`fs`/`fc` 로 지었다가 **`.fc` 가 이미 다른 컴포넌트에**
있었습니다(`display:flex`). 범례 점이 7×7 이 아니라 24×16 으로 그려졌습니다.
`check.py` 는 「CSS 에 없는 클래스」는 잡아도 **이미 있는 이름과 겹치는 것**은
못 잡습니다. `sgc`/`sgf`/`sgs` 로 바꾸고, 시험에 「다른 컴포넌트와 안 겹친다」를
넣어 뒀습니다.

### 시험

- `duepaneltest.js` 38건 신규 — collapse 단위 · 두 곳에 안 남음 · 지난 달 숨김 ·
  막대 합 100% · 42px 고정 · 일괄 버튼 없음.
- `duetest.js` · `staletest.js` 를 홈 패널로 옮겼습니다. 특히 `staletest` 는
  **어제치 캐시가 새 응답으로 덮이는지**를 재는 시험이라 지우면 안 됐습니다 —
  탭을 안 옮긴 채로 목록이 스스로 바뀌는지를 봅니다.
- `duetest.js` 의 `cardDue` 픽스처가 **실제 서버 모양과 달랐습니다**(`pay` 를
  카드 이름으로 넣어 뒀음 — 실제로는 결제일 `YYYY-MM-DD`). 시험은 통과하는데
  화면은 깨지는 종류라 같이 고쳤습니다.


## 1.26.0 — 2026-08-08

- 앱 `1.26.0` · Apps Script `변경 없음`
- 홈 위계 — 손익은 카드, 통장 흐름은 tonal 계산서 (**디자인 핸드오프 PART 1 · 앵커 10b**)

폴이 다른 창에서 만든 디자인 핸드오프를 받았습니다. **오늘 오후(1.24~1.25)에 한 것과
정면으로 부딪히는 부분이 있어** 폴 확인 뒤 「디자인대로」로 정했습니다.

### 왜 되돌리나 — 「지우기」가 아니라 「급을 낮추기」

폴이 헷갈린다고 한 건 **두 카드가 나란히 있는 것**이 아니라 **둘 다 같은 위계로
서 있던 것**이었습니다. 1.24.0 은 통장 흐름을 손익 카드 **안으로 접어** 없앴고,
디자인은 **아래 tonal 카드로 급을 낮춥니다.** 후자가 맞습니다 — 둘 다 매일 봐야
하는 숫자인데 하나를 접어두면 매번 눌러야 합니다.

| 1.24~1.25 | 1.26.0 (디자인) |
|---|---|
| 통장 흐름을 손익 카드 안으로 접음 | **별도 tonal 카드** (배경·테두리가 위계 장치) |
| 손익 → 현금 흐름 **조정식(다리)** | **들어온 돈에서 내려오는 계산서** |
| 막대그래프 제거 | **지출 구성 막대 부활** (다만 다른 걸 말함) |

### ① 계산서가 다리보다 나은 이유

1.24.0 다리는 **손익에서 출발**해서 현금 흐름까지 조정해 갔습니다. 그래서
두 자의 「들어온 것」 정의가 달라 **「그 밖에」 잔차 줄이 꼭 필요**했습니다.

```
들어온 돈                      3,693,325
−  바로 쓴 돈  체크·이체          566,100
−  지난달 카드값               1,755,130
−  이체 · 저축  쓴 건 아님      1,520,010
════════════════════════════════════════
=  남은 것                      −147,915
```

전부 **같은 현금 기준 안**이라 잔차가 생길 자리가 없습니다. 손익과의 차이는
맨 아래 **한 줄**로만 설명합니다 — 「이번 달 카드로 쓴 771,576원은 다음 달에
빠져나갑니다.」

- 「이체 · 저축」은 **밖으로 보낸 이체 + 저축·투자 + 대출 원리금** 셋을 묶습니다.
  ⚠️ 대출 원리금을 빠뜨리면 통장에선 나갔는데 어느 줄에도 없어서 **합이 안 맞습니다.**
- ⚠️ **차감 3줄 금액은 검정입니다.** 중간 줄을 빨강으로 칠하면 **저축까지 손해로
  읽힙니다.** 마지막 「남은 것」만 색을 씁니다.
- ⚠️ 부호 열은 **고정 폭 14px**. 안 그러면 숫자 시작점이 줄마다 흔들립니다.
- 접힘 상태는 **로컬에 저장**합니다. 안 그러면 수신함 하나만 확인해도 홈이 다시
  그려지면서 접힙니다.

### ② 막대는 되살리되 다른 걸 말합니다

1.25.0 에 「아래 두 칸과 같은 말」이라며 뺐던 자리입니다. 그 판단은 **그때 막대에
대해서는 맞았습니다** — 「수입 안 / 초과」는 수입·지출 두 칸의 되풀이였으니까요.

새 막대는 **고정 / 변동 / 남은 것** 세 토막입니다. 수입·지출 숫자만으로는 안 보이는
정보입니다.

- ⚠️ **분모는 `max(수입, 지출)`** 입니다. 수입으로만 나누면 지출이 수입을 넘는 달에
  막대가 칸을 뚫고 나갑니다. 넘는 달엔 「남은 것」이 0 이 되고 막대가 꽉 찹니다 —
  그게 「다 썼다」의 정확한 그림입니다.
- 0 인 토막은 아예 안 그립니다.

### ③ 「쓴 날짜 기준」

손익 카드 머리 오른쪽에 붙였습니다. ⚠️ **지우지 마세요** — 두 카드의 **기준이 다르다**는
걸 알리는 유일한 라벨입니다(디자인 1.4).

### 정리한 것

- `cashBridge`(조정식)·`cashRow` 를 통째로 걷어냈습니다. 디자인 2.6 의 **삭제 문구
  목록**에 있던 제 문구 여섯 개도 전부 사라졌는지 시험이 지킵니다.
- `bridgetest.js` 는 다리가 없어져 죽었습니다. 살릴 부분(세 갈래)만 떼어
  **`duesplittest.js`** 로 옮기고 나머지는 버렸습니다.
- ⚠️ **시험 러너의 판정이 틀려 있었습니다.** `node <파일> | tail -1` 로 봤는데
  마지막 줄이 **빈 줄**이라 실패를 놓쳤습니다. **종료 코드**로 바꿨습니다 —
  이 판에서 실제로 세 파일이 깨져 있던 걸 그제서야 찾았습니다.

시험 39건 (`cashcalctest`) — 절반이 「계산서가 산술적으로 닫히는가」입니다.

> 디자인 PART 2(앞으로 나갈 돈 인라인 패널)·PART 3(금액 가리기)는 다음 판입니다.


## 1.25.0 — 2026-08-08

- 앱 `1.25.0` · Apps Script `55`
- 이미 나간 카드값 빼기 · 맥박 배너는 앱에서만 · 홈 덜어내기

### ① 「아내 카드값은 이미 나갔는데 앞으로 나갈 금액으로 표시돼 있어」

맞았습니다. **고정비에는 `done` 판정이 있는데(`fixedSeen_`) 카드에는 없었습니다.**
`apiCardDue_` 는 계좌 시트의 결제일만 보고, 그 대금이 실제로 장부에 들어왔는지는
한 번도 안 봤습니다. 이미 빠져나간 돈이 며칠씩 「앞으로 나갈 돈」에 서 있었습니다.

판정은 **1.19.0 에 이미 만든 `cardBillHit_` 을 그대로 씁니다** — 결제수단이 통장인데
내용에 우리 카드 이름이 있으면 그건 대금입니다. 문구를 짐작하지 않고 계좌 시트에
등록된 사실로만 가릅니다.

- ⚠️ **금액은 안 봅니다.** 예상 청구액과 실제 결제액은 거의 늘 다릅니다(카드사마다
  이용기간이 달라서 화면에도 그렇게 적어 뒀습니다). 금액까지 맞추라고 하면
  **실제로 나간 건을 못 찾아 영영 「앞으로 나갈 돈」에 남습니다.**
- ⚠️⚠️ **걸러낸 뒤에 가장 가까운 결제일을 다시 고릅니다.** 예전 코드는 첫 카드의
  결제일을 못박고 그 날짜 것만 더했습니다. 걸러내기만 하고 순서를 안 바꾸면
  **그 날짜 카드가 전부 나갔을 때 합이 0 이 되면서 그 뒤 결제일을 통째로 잃습니다** —
  「앞으로 나갈 돈 0원」인데 실제로는 남아 있는 상태입니다.
- ⚠️ **목록에서 지우지 않고 「나갔어요」로 표시만** 합니다(흐리게 + 취소선).
  지우면 「내 카드값이 왜 목록에 없지」가 되고, 예상액과 실제 나간 금액을 눈으로
  대조할 근거도 같이 사라집니다. 실제 나간 금액(`paidAmt`)도 같이 보냅니다.
- 그룹 합계에서도 뺍니다 — 목록의 합과 위의 큰 숫자가 안 맞으면 세어보게 됩니다.
- 부제에 **결제일을 적습니다**(「카드 1,400,000 (8/25)」). 이번 달 것이 나가면 목록이
  다음 달 청구분으로 넘어가는데, 날짜가 없으면 「이미 낸 걸 왜 또」로 읽힙니다.

### ② 「웹브라우저에서도 띄워놨더니 알림 4시간 안들어왔다고 떴어. ㅋ」

**확인해 보니 브라우저가 원인이 아니었습니다.** 앱은 맥박을 한 번도 안 보내고
(`INBOX_HB` 는 폰 Automate 플로우만 갱신합니다), **4시간 공백은 진짜였습니다.**

진짜 문제는 **데스크톱에서는 그 배너로 할 수 있는 게 없다**는 것입니다 — Automate 를
되살리는 건 폰에서 하는 일입니다. 책상에서 화면만 보는데 빨간 배너가 뜨면 그건
알림이 아니라 소음입니다. 그래서 **홈 화면에 설치된 앱에서만** 띄웁니다.

- ⚠️ **정보를 없애는 게 아니라 자리를 옮기는 것입니다.** 브라우저에서도
  **설정 › 점검 › 알림 연결 확인**에서는 그대로 보입니다. 조용히 숨기기만 하면
  「왜 아무도 안 알려줬지」가 됩니다.
- `display-mode: standalone` + iOS `navigator.standalone` 둘 다 봅니다.
  판별할 게 없는 환경에서도 안 죽고 `false` 로 떨어집니다.

### ③ 「여전히 좀 복잡하다」 — 홈에서 셋을 걷어냈습니다

폴이 고른 세 가지입니다. 손익 카드가 **1092 → 827px** 로 줄어서 페이스가 한 화면에
들어옵니다.

- **손익 막대그래프 + 「수입 안 / 수입선」 범례** — 바로 아래 수입·지출 두 칸이 같은
  말을 더 정확하게 하고 있었습니다. 초과분은 배지(「지출 1.4배」)가 대신 말합니다.
- **「지난달 같은 날」 칸** — 페이스 카드에 「지난달 대비 −396,976」이 **같은 숫자**로
  이미 있었습니다. 한 화면에 같은 값이 두 번 있으면 어느 게 맞나 하고 한 번 더 봅니다.
- **「앞으로 나갈 돈」 세 갈래를 다시 접었습니다.** 홈은 한 줄, 세 갈래는 눌러서 들어가는
  내역 탭 상세 맨 위에 섭니다. 궁금할 때 보는 것이지 매번 볼 것이 아니었습니다.
  덤으로 1.24.0 의 스켈레톤 높이 문제도 사라졌습니다 — 한 줄은 줄 수가 안 변합니다.
- 부제의 고정비는 **금액 대신 건수**로 적습니다. 결제일을 넣으면서 390px 에서 두 줄로
  넘쳤는데, 둘 중 하나를 줄여야 한다면 **틀렸던 쪽(카드)** 이 남아야 합니다.

시험 38건 (`cardpaidtest`). ⚠️ `.hbar` 는 리포트 탭 지표 막대에도 쓰여서, 「지웠나」는
**손익 카드 안만 잘라서** 봅니다 — 파일 전체를 훑으면 남의 것이 걸려 거짓 실패가 납니다.

> 🔴 **재배포 필요.** `cardPaid_` 가 서버에 올라가야 이미 나간 카드값이 빠집니다.
> 순서는 상관없습니다 — 옛 서버는 `paid` 를 안 보내고, 앱은 없으면 예전처럼 답니다.


## 1.24.0 — 2026-08-08

- 앱 `1.24.0` · Apps Script `변경 없음`
- 현금 흐름을 손익 안으로 · 앞으로 나갈 돈 세 갈래 · 페이스 위로

### ① 「손익과 흐름이 비슷하게 있어서 되게 헷갈려 보여」

큰 숫자 두 개가 나란히 서 있고 **둘 다 「들어온 것 − 나간 것」 모양**이라 같은 걸
두 번 말하는 것처럼 보였습니다. 실제로는 같은 거래를 다른 자로 잰 것인데,
**둘이 어떻게 이어지는지가 화면에 없었습니다.**

「통장 흐름」 카드를 없애고 **손익 카드 맨 아래에 「현금 흐름」 한 줄로 접어** 넣었습니다.
접힌 채로도 금액은 보이고, 펼치면 **다리**가 나옵니다.

```
손익                        +2,355,649
 + 이번 달 카드로 긁은 것        771,576   지출엔 잡혔지만 통장에선 다음 달
 − 카드값                     1,755,130   지난달 긁은 것이 이번 달 통장에서
 − 밖으로 보낸 이체            1,500,010   나갔지만 쓴 건 아님
 − 저축·투자                      20,000
────────────────────────────────────────
현금 흐름                      −147,915
```

- ⚠️⚠️ **「그 밖에」 줄을 반드시 남깁니다.** 이 항등식은 두 자의 정의가 겹칠 때만
  딱 맞습니다 — 손익의 **수입**은 구분이 `수입` 인 것 전부지만, 흐름의
  **들어온 돈**은 `수입`·`차입`·`투자회수` 중 **계좌 시트에 등록된 입출금 통장**으로
  들어온 것만입니다. 8/8 화면은 우연히 같아서(3,693,325) 딱 맞아떨어졌지만
  **항상 맞지는 않습니다.** 안 맞는 만큼을 조용히 숨기면 「합이 왜 안 맞지」로
  남습니다. 그래서 잔차를 계산해 **그대로 한 줄로 드러냅니다.**
- ⚠️ **펼친 상태를 기억합니다.** 안 그러면 수신함 하나만 확인해도 홈이 다시
  그려지면서 접힙니다 — 보던 사람에겐 화면이 제멋대로 닫히는 걸로 보입니다.
- ⚠️ 펼친 안쪽에서 **들어온/나간 다섯 줄 쪼개기를 걷어냈습니다.** 다리가 카드값·
  이체·저축을 더 나은 말로 이미 적고 있어서 **같은 항목을 두 번** 말하고 있었습니다 —
  폴이 지적한 헷갈림과 같은 종류입니다. 다리에 없는 「그중 바로 쓴 돈」만 남겼습니다.
- 다리의 각 줄을 누르면 그 유형만 내역에서 봅니다. 「그 밖에」는 한 구분으로
  안 묶여서 **안 누르게** 뒀습니다 — 눌러서 엉뚱한 목록이 나오는 게 더 나쁩니다.

### ② 「앞으로 나갈 돈」 세 갈래

폴: 「앞으로 나갈 돈도 일부는 지출, 일부는 현금 흐름에 포함된 개념이라 이 부분도
구분지어서 분리해주는 게 나을까?」 — 맞고, **둘이 아니라 셋**이었습니다.

| | 손익(지출)에 | 현금 흐름에 |
|---|---|---|
| **카드값** | 이미 잡혔음 — 긁은 달에 | 앞으로 나감 |
| **고정비 · 지출 성격** | 앞으로 잡힘 | 앞으로 나감 |
| **고정비 · 저축·투자·빚** | **영영 안 잡힘** | 앞으로 나감 |

셋째가 폴이 짚은 자리입니다. 주담대 원리금이나 청약이 고정비에 있으면
**통장에선 나가는데 지출로는 영영 안 잡힙니다.**

- **서버는 안 건드립니다.** 대분류→구분 표가 이미 `boot.cats` 에 있고
  `fixedLeft.items[].cat` 도 넘어옵니다. 재배포가 필요 없습니다.
- ⚠️ 합계 판정을 **서버(`apiFixedLeft_`)와 같은 기준**(`!done && !skip`)으로 맞췄습니다.
  다르게 세면 세 줄의 합이 위의 큰 숫자와 안 맞습니다.
- ⚠️ **대분류를 못 찾으면 지출로 봅니다.** 고정비는 대부분 지출이고, 모르는 걸
  저축 쪽에 넣으면 **지출을 과소평가**합니다.
- 카드값 중 이번 달 청구분은 아직 쌓이는 중이라 **「아직 쌓이는 중」**을 적습니다.
  합에는 그대로 넣습니다 — 과소평가가 더 나쁩니다.
- 0 인 줄은 안 그립니다. 「저축·투자·빚 갚기 0원」이 늘 떠 있으면 없는 걱정을 만듭니다.
- 세 줄이 생기면서 부제(「카드 … · 고정 …」)는 **지웠습니다.** 같은 말을 두 번 하는
  데다 390px 에서 두 줄로 넘쳤습니다.

### ③ 페이스를 손익 바로 아래로

폴: 「나는 페이스 보는 것도 좋아서 위로 올라갔으면 좋겠거든.」
「이번 달 얼마나 벌고 썼나」 다음에 「그래서 페이스는 어떤가」가 오는 게 읽는
순서로도 자연스럽습니다.

### ⚠️ `duetest` 가 잡은 회귀

세 갈래 줄이 리포트 도착 후에 생겨서 **hero 높이가 368 → 448px 로 뛰었습니다.**
이 저장소가 이미 한 번 고친 「화면 출렁임」입니다. 스켈레톤이 **세 줄(최대치)**
자리를 미리 잡게 했습니다.

줄 수는 리포트가 오기 전엔 알 수 없으므로, 시험 기준도 「똑같다」에서
**「안 늘어난다」**로 바꿨습니다 — 늘어나면 아래 카드가 밀려 보던 자리가
사라지고, 줄어들면 위로 당겨질 뿐입니다(저장소가 이미 정해 둔 방향).
⚠️ 대신 **상한도 같이 잽니다.** 안 그러면 블록이 통째로 사라져도 시험이 통과합니다.

시험 38건 (`bridgetest`) — 절반이 「다리가 정확히 닿는가」와 「그 밖에가 숨지 않는가」입니다.
`cashtest`·`duetest` 는 옮겨간 자리를 따라가게 고쳤습니다.


## 1.23.0 — 2026-08-08

- 앱 `1.23.0` · Apps Script `54`
- 🔴 **인증 없이 열려 있던 옛 API 잠그기**

새 가족용 안내서를 쓰려고 코드를 훑다가 찾았습니다. **`gas/Code.js` 의 `route_` 에
토큰 검사가 한 줄도 없었습니다.**

배포 접근은 「모든 사용자(익명 포함)」이고 `/exec` 주소는 `app.js` 에 공개돼
있습니다. 즉 **주소만 아는 사람이면 누구나** 이걸 부를 수 있었습니다.

```
?api=undo&row=N   → 거래내역 행을 지운다
?api=add&...      → 거래내역에 행을 넣는다
?api=master       → 계좌 표시명·대분류·사람 이름
?api=summary      → 이번 달 수입·지출
?api=overview     → 월별 집계
```

2026-08-06 에 `Index.html` 을 지워 **화면**은 닫았는데(`doGet` 주석) **이 경로는
그대로 열려 있었습니다.** 문은 안 잠그고 간판만 뗀 셈입니다.

- 이제 `ping` 을 빼고 **전부 새 API 와 같은 토큰 검사**를 통과해야 합니다.
- `ping` 은 돌려주는 값이 `'pong'` 뿐이라 열어 둡니다 — 서버가 살아 있는지
  밖에서 볼 수 있어야 합니다.
- ⚠️ **새 API 는 여기서 막지 않습니다.** `apiRoute_` 가 먼저 가로채고 스스로
  인증합니다. 여기서 또 막으면 앱이 통째로 죽습니다.
- ⚠️ `verifyToken_` 은 `api.js` 에 있습니다. **그 파일이 없는 배포에서는
  열리는 쪽이 아니라 막히는 쪽으로** 떨어집니다.
- 지금 앱은 옛 API 를 **하나도 안 씁니다**(`add2`·`del`·`tx2`·`month`·`init`…).
  그래서 이 변경으로 앱 동작은 달라지지 않습니다.
- 시험 29건 (`legacyauthtest`). 「거절했다」만 보지 않고 **시트에 손이 닿았는지**로
  확인합니다 — 응답 문자열만 보면 막힌 척하고 지우는 코드를 못 잡습니다.

> 🔴 **재배포가 필수입니다.** 이 파일이 서버에 올라가야 잠깁니다.


## 1.22.0 — 2026-08-08

- 앱 `1.22.0` · Apps Script `변경 없음`
- 세션이 한 시간마다 끊기던 것

폴: 「조금만 있다 앱을 켜면 **무조건 세션 로그인 창이 떠서** 굉장히 불편하다.」

원인은 **구글 ID 토큰의 수명이 딱 한 시간**이라는 것입니다. 예전 코드는

1. 토큰이 **죽고 나서야** 다시 받으려 했고,
2. 그 사이 요청 하나가 실패하면 **곧바로 로그인 화면**을 띄웠고,
3. 조용히 다시 받아오는 데 성공해도 **실패한 요청을 다시 보내지 않았습니다.**

그래서 한 시간에 한 번씩, 화면에 이미 데이터가 떠 있는데도 로그인 창이 덮었습니다.

**고친 방향 셋**

- **① 미리 받는다** — 10분 남으면 조용히 갱신합니다. 앱을 앞으로 가져올 때,
  그리고 켜 둔 채로도 5분마다 봅니다. 여유가 있으면 구글을 아예 안 부릅니다.
- **② 기다린다** — 토큰이 없으면 로그인 화면 대신 **갱신을 걸고 요청을 그 갱신에
  태웁니다.** 여러 요청이 하나의 갱신을 같이 기다립니다(구글은 한 번만 부릅니다).
- **③ 정말 안 될 때만 보여준다** — 9초 안에 갱신이 안 끝나면 그때 로그인 화면.
  그 전까지는 보고 있던 화면 그대로입니다. 늦게라도 갱신되면 **저절로 닫힙니다.**

**⚠️ 조심한 것들**

- **구글 One Tap 은 사용자가 여러 번 닫으면 한동안 안 뜹니다(쿨다운).** 「갱신은 늘
  성공한다」고 가정하면 안 되므로 실패 경로를 반드시 남겼습니다 — 그게 로그인 화면입니다.
- **`promptPending` 이 영원히 안 풀리는 길을 막았습니다.** FedCM 을 쓰면 알림 콜백이
  안 올 때가 있는데, 그러면 그 뒤 갱신이 통째로 막혀 **로그인 창밖에 길이 없어집니다.**
  시간으로도 풉니다.
- **갱신일 때 `start()` 를 다시 부르지 않습니다.** 부르면 화면이 처음부터 다시
  그려져서 보고 있던 자리가 사라집니다.
- **401 은 한 번만 다시 보냅니다.** 401 은 「토큰이 죽었다」와 「등록 안 된 계정」이
  구분이 안 돼서, 무한히 돌면 안 됩니다. **쓰기는 아예 다시 안 보냅니다** —
  서버가 이미 처리했을 수 있어 행이 두 개 생깁니다.
- 시험 29건 (`authtest`). vm 안의 `Date` 가 바깥과 다른 물건이라 넘겨줍니다.


## 1.21.0 — 2026-08-08

- 앱 `1.21.0` · Apps Script `53`
- 입력 대기도 묶어서 한 번에 확인

**🔴 1.20.0 에서 묶기를 「내역」에만 넣은 게 잘못이었습니다.**

폴이 화면을 보내왔습니다 — 입력 대기에 **짱오락실 500원짜리가 열 건.** 「배포가
안됐나 안접히는데」. 배포는 됐고, 접기가 **장부에 이미 들어간 줄**에만 걸려 있었던
겁니다. 그런데 성가신 건 「다 넣고 나서 줄이 열 개 보이는 것」이 아니라
**「확인을 열 번 누르는 것」**이었습니다. 일이 실제로 쌓이는 자리에 없었습니다.

이제 입력 대기에서도 같은 기준(같은 날·같은 가게·같은 결제수단, 3건부터)으로 접힙니다.

```
짱오락실 동탄타임테  ×10                    5,000
고니 폰 · 8/8 · 토스부부 · 문화/여가
[모두 무시]  [한 줄로 확인]          따로 보기
```

- **「한 줄로 확인」** → 장부에 **5,000원 한 줄**. 수신함 10줄은 전부 「등록」으로
  찍히고 **같은 거래행**을 가리킵니다.
- ⚠️ **원본 금액(500)을 합계로 덮지 않습니다.** 열 줄이 5,000원 열 줄로 바뀌면
  카드 명세서와 대조할 근거가 통째로 사라집니다. 묶음일 때는 **상태·거래행만** 찍습니다.
  (낱건 하나일 때는 예전대로 화면에서 고친 값이 수신함에도 반영됩니다.)
- ⚠️ **일부만 이미 등록돼 있으면 통째로 멈춥니다.** 그대로 진행하면 그 결제가
  장부에 두 번 들어갑니다 — 다른 기기에서 먼저 처리했을 때 실제로 납니다.
  전부 등록돼 있으면(두 번 누른 것) 조용히 성공으로 넘깁니다.
- ⚠️ **취소보류는 절대 안 묶입니다.** 섞이면 취소된 결제가 묻혀서 장부에 들어갑니다.
- ⚠️ 멱등키는 **맨 위 줄 하나**(`inbox<첫 행>`). 같은 묶음을 두 번 보내도 한 건입니다.
- 제목에 건수를 박습니다(「결제 10건 확인」) — 5,000원이 어디서 나온 숫자인지
  화면에 안 적히면 「내가 500원짜리를 눌렀는데?」가 됩니다.
- **「따로 보기」**로 펼치면 낱건을 따로 처리할 수 있습니다 (하나만 취소됐다든가).
- ⚠️ 버튼 셋을 낱건처럼 오른쪽에 붙였더니 **360px 에서 가게 이름이 통째로
  사라졌습니다** (`.l` 이 `min-width:0` 이라 0 까지 눌립니다). 버튼은 아랫줄로 뺐습니다.
- `inboxOk`·`inboxNo` 둘 다 `rows` 를 받습니다. 옛 앱이 보내는 `row` 하나도 그대로 됩니다.
- 시험 55건 (`inboxbulktest`) — 대부분이 「두 번 들어가는 길」과 「원본이 사라지는 길」입니다.

> 🟡 **재배포를 먼저.** `rows` 를 모르는 서버에 새 앱이 먼저 올라가면
> 묶음 확인이 `bad row` 로 실패합니다.


## 1.20.0 — 2026-08-08

- 앱 `1.20.0` · Apps Script `52`
- 통장 흐름 · 반복 결제 묶기 · 폰 이름 인식

### ① 홈에 「이번 달 통장 흐름」

폴: 「수입이 잡힌 건 알겠고, 카드값이 이체로 빠지는 것도 알겠고, 지출도 쓴 돈만인
것까지도 알겠는데 **남은 돈 계산이 안되네?**」

버그가 아니라 **답한 적이 없던 것**입니다. `CASH_G = { '수입': 1, '지출': 1 }` 때문에
손익은 「이번 달 얼마나 썼나」를 냅니다. 통장 기준과는 딱 세 군데서 갈라집니다.

| | 통장에서 나갔나 | 지출로 잡히나 |
|---|---|---|
| 카드로 긁은 것 | 아직 (다음 달) | 지금 잡힘 |
| 카드 대금 결제 | 지금 나감 | 안 잡힘 (이체) |
| 저축·투자·대출 원금 | 지금 나감 | 안 잡힘 |

그래서 같은 거래내역을 **결제수단 기준으로** 다시 세는 카드를 손익 밑에 붙였습니다.
들어온 돈 · 나간 돈 · 바로 쓴 돈 / 카드값 / 저축·투자 / 대출 원리금 / 밖으로 보낸 이체.
줄을 누르면 그 유형만 내역에서 봅니다.

- **서버는 안 건드립니다.** 앱이 `tx2` 응답만으로 계산합니다.
- 「남은 돈」이라 안 씁니다 — **시작 잔액이 어디에도 없어서 잔액이 아닙니다.**
  계좌 시트엔 잔액 칸이 없고 자산 시트는 계좌 단위가 아닌 월별 스냅샷입니다.
  낼 수 있는 건 「이번 달 통장에 들고 난 차액」까지입니다.
- **내 통장(입출금)끼리 옮긴 건 뺍니다.** 저축·청약으로 보낸 건 우리 돈이지만
  이 달에 쓸 수 있는 돈에선 빠졌으므로 「나간 돈」입니다 — 구분을 `이체` 로 넣든
  `저축/투자` 로 넣든 답이 같아야 합니다.
- 뺀 것·못 센 것(계좌 시트에 없는 결제수단, 자본거래)은 **건수와 금액을 화면에 적습니다.**
  조용히 빼면 「합이 왜 안 맞지」로 남습니다.

### ② 같은 가게 반복 결제 묶기

폴: 「오락실 같은데서 소액 결제를 반복해서 하면 내역에 건이 많이 쌓이는 게 별로거든.」

같은 날·같은 내용·같은 결제수단이 **3건 이상**이면 내역에서 한 줄로 접습니다
(`오락실 ×5 · 12,000`). 눌러서 펼치면 낱건이 다 보이고 「한 줄로 합치기」가 있습니다.

폴 선택: 화면만이 아니라 **시트 원본도 한 줄로** 합칩니다. 되돌릴 수 없고 카드
명세서 대조가 안 되는 게 그 대가라, 지우기 전에 **원본 줄을 통째로 「묶음로그」
시트에 옮겨 적습니다.**

- 순서가 전부입니다 — 읽고 → **서버가 같은 건인지 다시 확인**하고 → 로그에 적고 →
  `flush` 로 굳히고 → 그제서야 지웁니다. 지운 뒤 적으려다 실패하면 되돌릴 길이 없습니다.
- **아래에서부터 지웁니다.** 위에서부터 지우면 남은 번호가 밀려 엉뚱한 줄이 죽습니다.
- 같은 행이 두 번 들어오면 걸러냅니다 (금액이 부풀고 엉뚱한 줄이 지워집니다).
- 합친 뒤 앱은 **반드시 다시 받습니다** — 행 번호가 통째로 밀렸습니다.
- 두 번 눌러도 한 번만 갑니다 (`mergeBusy`).

### ③ 「폰 미상」 — 이름을 바꿨더니 폰이 안 따라왔다

폴: 「아내 폰으로 잡은건데 폰 미상으로 잡히네.」

8/5에 두 폰 플로우를 `&w=아내` · `&w=폴` 로 만들었고, **8/7에 사람 이름을
「폴·아내」에서 「고미·고니」로 바꿨습니다.** `inbox_who_` 는 설정 시트 사용자 목록에
정확히 있는 이름만 통과시키므로, 그 뒤 아내 폰 알림이 전부 버려져 「폰 미상」으로
쌓였습니다. **버렸다는 기록조차 안 남겨서 사흘을 몰랐습니다.**

- `inbox_who_` 가 애칭 표(`PERSON_ALIAS`)로 **옛 이름을 이어줍니다.** 표를 어느 방향으로
  적어도 찾습니다. 폰은 안 고쳐도 됩니다.
- **못 알아들은 이름은 맥박에 `?이름` 으로 남깁니다.** 건강 진단에 「모르는 이름: …」로
  뜨고, 「w 를 아예 안 보낸다」와 구분됩니다 — 고칠 곳이 다릅니다.
- 그 줄은 사람 명단에서 빼야 합니다. 안 그러면 「모르는 이름: … 폰이 3시간
  조용해요」 같은 엉뚱한 판정이 나옵니다.
- 실명·별명은 **코드에 안 넣습니다** (저장소 Public). 스크립트 속성에만.
- 덤: `.nhr.bad` 에 색이 없어서 「아직 없음」 줄이 멀쩡한 줄과 똑같이 보이던 것도 고쳤습니다.

### 시험대

`cashtest` 48 · `mergetest` 52 · `whonametest` 26 — 새로 126건 붙였습니다.


## 1.19.0 — 2026-08-08

- 앱 `1.19.0` · Apps Script `51`
- 카드값을 지출로 넣으면 막고 물어본다

폴: 「아내가 우리카드값을 기타로 등록했어. 등록할 때 감지해서 그대로 입력 못하게
해야할 것 같은데.」 → **재보니 딱 1건이었고 8월 지출이 1,255,130원 부풀어 있었습니다**
(거래내역 4279행, `이체 / 계좌이체` 로 고침). 카드로 긁은 것들은 이미 각각 지출로
들어가 있으니 대금까지 지출로 넣으면 같은 돈을 두 번 셉니다.

- ⭐ **물건을 산 것과 대금은 결제수단만 보면 갈립니다 — 문구를 짐작하지 않습니다.**
  · 결제수단이 **카드** → 물건을 산 것. **아예 안 봅니다.**
  · 결제수단이 **통장**인데 내용에 우리 카드 이름 → 결제일에 빠진 대금.
  계좌 시트의 종류(E열)로 카드를 알아내므로 **등록된 사실로만** 판정합니다.
  폴 확인: 「이건 월 결제 건만 이체로 넣는 얘기」 — 그 달에 그 카드로 뭘 사든
  한 건도 안 막힙니다.
- ⚠️ **아주 막지는 않습니다.** 카드사로 나가는 돈이 전부 대금은 아닙니다 —
  **연회비·이자·수수료는 진짜 지출**입니다. 막기만 하면 「왜 저장이 안 되지」 하다가
  엉뚱한 카테고리로 우회하게 됩니다. 그래서 고르게 합니다:
  **「이체로 넣기(권장)」 / 「연회비·이자예요 · 그대로 지출로」**
- ⚠️ `force` 는 **한 번만** 듣습니다. 폼에 남겨두면 그 뒤 저장까지 계속 뚫려서
  규칙이 있으나 마나가 됩니다.
- ⚠️ 어느 쪽을 고르든 **저장은 사람이 다시 누릅니다.** 조용히 저장하면 무엇이
  들어갔는지 못 보고 지나갑니다.
- ⚠️ 거절당하면 수신함을 **「등록」으로 안 찍습니다.** 장부엔 없는데 수신함만
  끝난 것처럼 보이면 그 결제는 영영 안 들어갑니다.
- ⚠️ 거절 응답의 **겉껍데기는 `ok:true`** 입니다(`{ok:true,data:{ok:false,cardBill}}`).
  앱이 `data` 를 안 보면 화면엔 저장된 것처럼 남고 장부엔 없습니다.
- ⚠️ 확인 화면 클래스는 **`app.css` 에 있는 것만** 씁니다. 설치 안내 페이지의
  `.note`·`.warn` 을 가져다 썼다가 `check.py` 에 걸렸습니다 — 서식 없는 글자가 됐을 겁니다.
- 시험 25건 (`cardbilltest`) — 절반이 「카드로 긁은 건 막으면 안 된다」 쪽입니다.


## 1.18.1 — 2026-08-08

- 앱 `1.18.1` · Apps Script `변경 없음`
- 맥박 문턱 — 낮 3시간 / 밤낮 통틀어 12시간

**🔴 실제로 놓친 사고.** 폴: 「알림왔는데 반영이 안되네. 오토메이트도 돌고 있는데」

Automate 로그: `03:00:16` 마지막 → **8시간 52분 공백** → `11:52:52 Resumed after
restart`. 그 사이 **11:33·11:46 에 네이버페이 결제 두 건**(36,000 / 38,500)이
떴습니다. Automate 의 「Notification posted?」는 **새로 뜨는** 알림만 잡으므로,
플로우가 되살아나도 이미 알림창에 있던 그 둘은 **영영 안 옵니다.**

**문턱이 12시간 하나뿐이라 배너가 조용했습니다.** 8시간 52분은 12시간에 못 미칩니다
— 아침 반나절 지출이 통째로 사라졌는데 화면은 멀쩡했습니다.

- **낮(09~22시)에 3시간** 조용하면 뜹니다. 밤낮 통틀어 12시간이면 그대로 뜹니다.
- ⚠️ **「지금이 낮인가」로 재면 안 됩니다.** 밤새 조용한 건 정상인데, 아침 9시가
  되는 순간 11시간짜리 밤 공백이 낮 기준에 걸려 **매일 아침 배너가 뜹니다.**
  `hbDayGap()` 이 마지막 수신 이후 흐른 시간 중 **낮에 해당하는 만큼만** 셉니다.
- 5분씩 훑습니다. 15분으로 재봤더니 오늘 건이 **7분 일찍** 걸렸습니다 —
  토막이 클수록 문턱을 일찍 넘습니다.
- 배너 문구에 **「다시 살아나도 그 사이 알림은 안 들어옵니다」**를 넣었습니다.
  안 적으면 「곧 들어오겠지」 하고 기다리게 됩니다.
- ⚠️ 홈 배너와 「알림 연결 확인」 화면이 **같은 규칙**을 씁니다. 한쪽만 고치면
  배너는 「끊겼어요」, 확인 화면은 「살아 있어요」라고 말합니다.
- 시험 27건 (`hbgaptest`) — 오늘 있었던 일을 그대로 넣어 재현합니다.

> 폰 쪽 진짜 원인은 안드로이드 절전입니다. Automate 를 **배터리 제한 없음**으로
> 두어야 합니다. 앱은 끊긴 걸 **빨리 알려줄 수만** 있지 막지는 못합니다.


## 1.18.0 — 2026-08-08

- 앱 `1.18.0` · Apps Script `50`
- 목표 화면 입구 늘리기 — 카테고리 카드 헤더 · 이 달 목표 미설정 배너

폴: 「목표 금액 수정하는 진입점이 너무 제한적이다. 나 아니면 찾기 힘들 것 같아.」

- **① 「카테고리 · 예산 대비」 카드 헤더에 「목표 ✎」.** 줄마다 `/ 예산`이 적혀
  있는데 고칠 길이 없었습니다. 예산 숫자를 보고 있는 자리가 맞습니다.
- **② 홈 배너 「2026년 8월 목표를 아직 안 정했어요」.** 버튼만 늘리면 **찾아갈
  생각을 한 사람**만 찾습니다. 달이 바뀌면 앱이 먼저 말을 겁니다.
  「목표 짜기」 / 「이 달은 숨기기」.
- ⚠️ **배너 판정을 `budChanged` 로 하면 안 됩니다.** 그건 「예산 시트와 다른가」라
  7월에 바꾼 값이 8월로 이어지기만 해도 켜져 있습니다. 서버에 `budSetFor_(ym)`
  (적용월이 **딱** 그 달인 줄이 있나)를 새로 만들어 `month.budget.setThis` 로
  싣습니다.
- ⭐ 서버가 아직 옛 판이라 `setThis` 가 없으면 **배너를 안 띄웁니다.** 모르는 걸
  「안 정했다」고 단정하면 매일 거짓말하는 배너가 됩니다.
- 지나간 달을 보고 있을 땐 안 뜹니다(이제 못 바꾸는 달입니다). 숨기기는 그 달에만
  듣고 **다음 달엔 다시 뜹니다.**
- ⚠️ 배너 색은 **하늘색**입니다. 알림 끊김(빨강)은 사고고 이건 할 일입니다.
  매달 한 번 뜨는 배너가 빨갛게 야단치면 곧 안 보게 됩니다.
- ⚠️ 입구가 셋이 되면서 `$('button[data-a="bud"]')` 는 **첫 하나만** 잡아 나머지가
  죽은 버튼이 됐습니다. `bindBudEntries()` 한 곳으로 모아 전부 물립니다.
  배너는 「숨기기」만 처리합니다 — 양쪽에서 열면 화면이 두 번 뜹니다.
- 시험 53건 추가 (`budentrytest` 34 · `entryuitest` 19), 저장소 전체 509건.

> 🟡 **재배포를 먼저.** `setThis` 가 없는 서버에서는 배너가 안 뜹니다(안 죽습니다).
> 카드 헤더 입구는 서버와 무관하게 바로 됩니다.


## 1.17.0 — 2026-08-08

- 앱 `1.17.0` · Apps Script `49`
- 월별 목표 금액 화면 — 지난달 실적으로 채우기 · 다음 달 미리 짜기

폴: 「카테고리별 목표 금액을 지난달 기준으로 잡아줘. 그리고 월별 목표 금액 작성
화면도 따로 있어야 할 것 같아. 디폴트는 지난달 목표 금액으로 두고 직접 수정할 수
있도록.」

- **디폴트는 원래부터 지난달 목표였습니다** — `budOver_` 가 적용월 ≤ ym 을 누적해서
  덮기 때문입니다. 그래서 새로 만든 건 **「지난달에 실제로 얼마 썼나」** 하나입니다.
- 새 API `budgetPlan` — 대상 달의 목표(eff·base·order) + **마지막으로 끝난 달의
  카테고리별 실제 지출**. ⚠️ `month` 응답의 `cats` 는 **상위 8개, 쓴 것만** 담고
  있어서 목표를 짜는 데 못 씁니다.
- 화면: **이번 달 / 다음 달** 탭 · 칸마다 지난달 실적을 회색으로(눌러서 그 칸만
  가져오기) · 「2026년 7월 실적으로 채우기」 한 방 · 「되돌리기」.
- ⚠️ **「지난달」이라고만 적지 않고 어느 달인지 이름을 박습니다.** 다음 달을 짤 때
  실적은 「직전 달」이 아니라 마지막으로 끝난 달입니다 — 진행 중인 달을 실적이라
  부르면 7일치가 한 달치로 둔갑해 목표가 반 토막 납니다.
- ⭐ 「실적으로 채우기」는 **실적이 없는 칸을 0 으로** 둡니다. 옛 목표를 남겨두면
  버튼에 적힌 금액과 총액이 안 맞아 고장 난 것처럼 보입니다.
- **지난 달은 못 바꿉니다** — 소급하면 이미 본 리포트의 숫자가 나중에 달라집니다.
  화면에 탭이 없고, **서버(`budgetSet_`)에서도 막습니다.** 화면에서만 막으면
  그건 규칙이 아닙니다.
- 안 저장한 채로 달을 바꾸려 하면 막습니다. 조용히 버리지 않습니다.
- 설정의 「이달의 목표액」 → **「월별 목표 금액」**.
- 시험 161건 (`budplantest` 46 · `budtest` 68 · `buduitest` 47).

> 🟡 **재배포를 먼저.** `budgetPlan` 이 없는 서버에 새 앱이 먼저 올라가면 화면이
> 옛 방식(이번 달만)으로 열립니다 — 읽기라 죽지는 않지만 달 탭도 실적도 없습니다.


## 1.16.0 — 2026-08-07

- 앱 `1.16.0` · Apps Script **재배포 필요** (`api.js` 변경)
- **🔴 정정.** 종합 진단 ①번이 「유동부채 만기를 확인해 보세요 — 분류가 잘못됐을 수 있고,
  그러면 유동비율 문제는 한 푼도 안 모으고 사라집니다」라고 말하고 있었습니다.
  **시트를 안 보고 한 추측이었고 틀렸습니다.** 부채 시트엔 만기일(H열)이 처음부터 있었고,
  값은 이렇습니다 — 안심전환 2052-11-30(26년) · **토스뱅크대환 2027-05-06(8.9개월)** ·
  폴스타 할부 2029-12-10(3.3년). **분류는 정확했고 유동비율 16.7% 는 진짜 상황입니다.**
- `debtDueSoon_()` 신설 — 부채 시트에서 만기가 1년 안에 오는 줄을 골라 `report2` 에 싣습니다
  (이름·잔액·금리·월상환액·만기일·남은 날수·메모). 최신 기준월 줄만, 가까운 것부터.
- ①번이 이제 이렇게 말합니다: 「토스뱅크대환대출 6,810만원을 어떻게 할지 정하세요 ·
  2027-05-06 · 8.9개월 뒤」 + 메모의 「이자만 납부」를 읽고 **원금이 통째로 온다**고 경고.
  만기일이 비어 있으면 「부채 시트에 적어 주세요」로 바뀝니다.


## 1.15.0 — 2026-08-07

- 앱 `1.15.0` · Apps Script **재배포 필요** (`api.js`·`import.js` 변경)
- 폴: 「다음달에는 진단 결과가 알아서 나온다는거지?」 → **아니었습니다.**
  `월스냅샷()` 은 자산·부채 시트를 **읽기만** 하고(자산추이에 요약 한 줄만 씁니다),
  새 달 줄은 사람이 만듭니다. 자산 시트 2행에 규칙이 적혀 있습니다 —
  「매달 1일에 지난달 줄을 복사 → 기준월만 바꾸고 잔액 최신화.」
- **가계부 › 이번 달 자산·부채 줄 만들기** — 지난달 줄을 통째로 복사하고 **A열(기준월)만**
  바꿉니다. 컬럼 이름을 가정하지 않아 시트 모양이 바뀌어도 안 깨집니다.
  이미 이번 달 줄이 있으면 아무것도 안 만듭니다(두 번 눌러도 안전).
  평가액·잔액은 은행·증권 앱을 봐야 아는 값이라 **지난달 값 그대로 복사되고, 폴이 고칩니다** —
  확인창과 완료창에서 두 번 경고합니다.
- **1.14.0 배너에 구멍이 있었습니다.** 「기준월(C3)만 뒤처짐」만 잡아서, 자산 시트 자체를
  안 적으면 `asOf`·`asOfLatest` 가 둘 다 지난달이라 **조용했습니다.**
  이제 「이번 달 자산·부채를 아직 안 적었어요」도 잡습니다(달이 바뀌면 바로).


## 1.14.0 — 2026-08-07

- 앱 `1.14.0` · Apps Script **재배포 필요** (`api.js`·`import.js` 변경)
- 폴: 「종합진단 갱신은 어떻게 되는거야?」 → 사슬을 따라가 보니 **끊기는 자리**가 있었다.
  「재무상태표」 시트의 C3「기준월」은 **손으로 고르는 드롭다운**이고 `월스냅샷()` 은
  그걸 안 건드린다. 9월 자료가 다 들어와도 C3 가 2026-08 이면 앱은 8월 숫자를
  계속 보여주면서 **겉으론 멀쩡해 보인다.** 제일 나쁜 종류의 고장.
- `bsYmSync_()` — 자산 시트의 최신 기준월을 찾아 C3 에 넣는다. **앞으로 당기기만 한다**
  (지난 달을 보려고 일부러 내려놨을 때 도로 밀어 올리면 화면이 사람 손을 이긴다).
  시트를 열 때(`onOpen`) 조용히 한 번 + 「가계부 › 재무상태표 기준월 최신으로」.
- `report2` 응답에 `balance.asOfLatest` 추가. 앱이 낡음을 판정해 리포트 맨 위에 배너.
- 「약 N개월」의 근거가 추정치면 밝힌다. 자산추이 13줄 중 12줄이 Phase0 에서 **역산한
  추정치**다(2026-08 만 실측). 숨기면 없는 정확도를 파는 것이다. 실측이 쌓이면 저절로 사라진다.


## 1.13.0 — 2026-08-07

- 앱 `1.13.0` · Apps Script `변경 없음`
- **건전성 「종합 진단」 카드 신설.** 처방을 지표 줄마다 흩어놓지 않고 한 곳에 모았다.
  폴: 「항목마다 각각 금액을 계산·추천해주고 있어서 혼란스럽거든.」
- ⚠️ 지표별 금액은 **더하면 안 되는 숫자들**이었다. 현금성 3개월에 필요한 995만원은
  유동비율 100%에 필요한 5,673만원 **안에 이미 들어 있다.** 둘 다 「유동자산을 더
  모아라」는 같은 말이다. 나란히 놓으니 6,668만원이 필요한 것처럼 읽혔다.
- 이제 한 줄로 합친다: `필요한 유동자산 = max(3개월치 지출, 유동부채)` ·
  `모아야 할 돈 = 그것 − 지금 유동자산`. 화면엔 이 **한 금액**만 크게 뜬다.
- 그 아래 효과 순서대로 단계를 적는다:
  ① 유동부채 만기 확인(0원 — 분류가 문제면 한 푼도 안 모으고 사라진다)
  ② 현금성 3개월 ③ 유동비율 100%(②를 포함한 누적) ④ 부채비율(원금상환이 해결, 할 일 없음)
- 건전성 표는 값·막대·전월 대비만 남기고 처방 문구를 걷어냈다.


## 1.12.1 — 2026-08-07

- 앱 `1.12.1` · Apps Script `변경 없음`
- **페이스 차트 범례가 거짓말하고 있었다.** 「한 달」 모드의 회색선은 지난달 **전체**인데
  범례는 언제나 「지난달 같은 기간」이라고 적혀 있었다. 7일치 빨간 토막 옆에 31일치
  회색선을 세워놓고 같은 기간이라 부른 것 — 「이번 달은 엄청 적게 썼다」로 읽힌다.
  폴: 「그래프 왜이래」
- 이제 「한 달」 모드에서 지난달을 **오늘까지(진한)와 그 뒤(옅은)로 나눠** 그리고,
  같은 기간이 끝나는 자리에 회색 점을 찍는다. 이번 달 점과 세로로 나란히 서서
  「지난달 대비」 숫자가 어느 두 점의 차이인지 눈으로 보인다.
- 범례 문구도 모드에 따라 바뀐다 — 경과일 「지난달 같은 기간」 / 한 달 「지난달 (진한 데까지가 같은 기간)」
- 늘려 맞출 때 곱하는 값을 이번 달 날수(`dim`)에서 **지난달 날수(`pd`)** 로 고쳤다.
  31일 달에서 30일 달을 볼 때 끝이 잘리고 중간이 밀렸다.


## 1.12.0 — 2026-08-07

- 앱 `1.12.0` · Apps Script **재배포 필요** (`api.js` 변경)
- **건전성** — 지표마다 「얼마가 모자란지 · 지금 속도면 언제 닿는지」를 적는다.
  권장선은 일반 기준 그대로 두고(폴 결정), 주담대가 있어 높게 나오는 이유를
  설명으로 붙였다. 부채비율·자기자본비율은 전월 대비도 같이.
- **이달의 목표액** — 총액과 카테고리별 목표를 앱에서 바꾼다. 바꾼 달부터 계속
  적용되고, 지난 달을 열면 그때 기준으로 보인다.
  새 시트 **「예산변경」**(A 적용월 · B 대분류 · C 금액 · D 바꾼 사람 · E 바꾼 시각)이
  자동으로 만들어진다. 「예산」 시트는 안 건드린다 — 줄을 지우면 원래대로 돌아온다.
  총액은 저장하지 않는다(카테고리 합이 곧 총액).
- 새 API `budgetSet` · `month` 응답에 `budget`·`pace.baseBudget`·`pace.budChanged` 추가


## 1.11.28 — 2026-08-07

- 앱 `1.11.28` · Apps Script `변경 없음`
- 수신함 알림에 누구 폰인지 표시


## 1.11.27 — 2026-08-07

- 앱 `1.11.27` · Apps Script **재배포 필요**
- **리포트가 새로 와도 홈·내역이 다시 안 그려지던 것.** 이게 진짜 원인이었습니다.
  `loadReport` 가 `ST.tab === 'report'` 일 때만 `render()` 를 불렀습니다.
  리포트 응답은 홈(「앞으로 나갈 돈」)과 내역(카드 결제·고정지출)도 쓰는데
  그 두 화면은 새 응답을 받고도 그대로 있었습니다. 그래서 localStorage 에 남은
  **어제치**가 계속 보였습니다 — 1.11.26 에서 서버를 고쳤는데도 폴 화면에는
  청약저축이 [등록] 인 채로 남아 있던 게 이것입니다.
  **서버를 아무리 고쳐도 안 보이는 종류의 버그입니다.**
  받아오는 조건도 `!ST.rep` 에서 `repRefresh()`(없거나 60초 지났으면) 로 모았습니다 —
  캐시가 있으면 영원히 안 받는 상태였습니다.
- **폴·아내 → 고미·고니.** 폴: 「아내는 내 입장이고. 고미 고니로 하자, 내가 고미야.」
  「아내」는 폴 쪽에서 본 호칭이라 고니가 직접 쓰는 앱에 띄울 말이 아닙니다.
  · 이름은 이제 `api.js` 의 `PEOPLE` **한 줄에서만** 정합니다. 코드 18곳에
    박혀 있던 문자열을 전부 걷어냈습니다. 앱은 `boot.people` 의 **자리**로
    색과 순서를 정하므로 이름이 바뀌어도 안 깨집니다.
  · 실명(승화·고은)은 **안 씁니다.** 저장소가 Public 이고 바로 옆에 이메일이
    있어서, 실명을 넣으면 실명과 이메일이 나란히 공개로 남습니다.
  · 시트 데이터용으로 `이름 바꾸기 점검`(읽기만)과 `이름 바꾸기 적용`(백업 + 확인창)을
    메뉴에 넣었습니다. 계좌·자산·설정·수신함·거래내역·임포트 여섯 자리를 한 번에 바꿉니다.
    ⚠️ **칸 전체가 정확히 그 이름일 때만** 바꿉니다 — 부분치환하면 「폴스타」가
    「고미스타」가 됩니다. 테스트로 못 박았습니다.


## 1.11.26 — 2026-08-07

- 앱 `1.11.26` · Apps Script **재배포 필요** (`report2` 경로가 바뀝니다)
- **청약저축이 [등록]으로 계속 되돌아오던 것.** 폴: 「등록했는데 다시 돌아왔네?」
  저장은 됐습니다 — 거래내역 4269행에 있었습니다. 판정이 못 본 것입니다.
  `fixedSeen_` 이 **구분이 「지출」인 행만** 봤는데, 청약저축은 자본거래라
  「저축/투자」로 들어갑니다. 그렇게 넣는 게 맞는데 그 순간 판정에서 빠졌습니다.
  묻는 것은 「이 고정비가 이번 달 장부에 들어갔나」지 「지출로 들어갔나」가
  아닙니다. 이제 수입만 빼고 다 봅니다.
- **거르기 칩을 언제나 금액으로.** 폴: 「건수일거면 다 건수던가. 어떤건 금액이고
  어떤건 건수인 기준이 뭐야?」 예전엔 지출 금액만 세서, 수입 카테고리는 합이
  0 이라 슬그머니 「4건」으로 바뀌었습니다. **규칙이 화면에 안 보이면 그건
  규칙이 아니라 사고입니다.** 이제 지출·수입·그 밖을 따로 담고 언제나 금액을
  보여줍니다(수입은 `+` 를 붙여 방향 표시). 금액이 아예 없을 때만 건수입니다.
  결제수단 묶음 머리줄도 같은 규칙을 씁니다.
- 카드부채점검 ⑥ 을 「아내 계좌 움직임」으로 고치고 수입·그 외 건수를 요약합니다.
  첫 판에서 **수입만** 찍혔습니다 — 아내분 폰이 입금 알림만 보내고 출금 알림은
  안 보냅니다. 카드값만이 아니라 아내분 계좌의 **모든 출금**이 빠져 있습니다.


## 1.11.25 — 2026-08-06

- 앱 `1.11.25` · Apps Script `변경 없음` (편집기 반영만 필요)
- 카드부채점검에 두 항목 추가. 둘 다 **폴이 짚어준 것을 숫자로 거는 것**입니다.
  · **⑤ 카드값 갭을 간편결제가 메우나** — 폴: 「현대카드 갭은 네이버페이·
    카카오페이로 긁은 것.」 맞다면 집 전체 지출은 맞고 **카드별 배분만** 틀린
    것이라 고치는 방법이 완전히 달라집니다. 갭(나간 카드값 − 그 달 그 카드 지출)을
    **같은 사람 명의** 간편결제 지출과 견주고, 모자라면 얼마가 모자란지 적습니다.
  · **⑥ 아내 계좌에서 나간 이체(5만원↑)** — 폴: 「자동이체 돼서 내역에
    반영됐을텐데 이상하네.」 이름으로 찾으면 못 찾습니다 — **없는 것을 이름으로는
    못 찾으니까요.** 아내 소유 계좌에서 나간 이체를 통째로 폅니다. 한 건도 없으면
    「(한 건도 없습니다)」라고 못박습니다 — 빈 표는 「없음」과 다릅니다.


## 1.11.24 — 2026-08-06

- 앱 `1.11.24` · Apps Script `변경 없음` (편집기 반영만 필요)
- 카드부채점검 ④ 청구월이 날짜로 삼켜져 `46,204` 로 찍히던 것.
  시트는 `setValues` 하는 순간 글자를 보고 날짜인지 숫자인지 **스스로 정합니다.**
  `'2026-07'` 이 날짜(일련번호 46204)로 삼켜진 위에 `#,##0` 이 덮였습니다.
  서식을 값보다 **먼저** 박도록 바꿨고, 순서가 뒤집히면 실패하는 테스트를 붙였습니다.
- 첫 실행으로 알아낸 것 (2026-08-06 · 미결제 추정 2,303,242):
  · 우리카드 — 「전달 1일~말일」 모델이 **정확히** 맞는다. 최근 4달 차이 0%.
    두 건으로 쪼개져 나간 달도 합이 1원도 안 틀린다(3,479+119,021 = 122,500).
  · 삼성카드 — 매달 1,263,626(폴스타 할부)은 「지출」이 아니라 「이체」로만 나간다.
    **부채 시트와 겹치지 않는다.** 카드 사용분도 따로 정확하다(0.1%).
  · 현대카드 — 나간 돈이 기록된 지출보다 훨씬 크다(2025-12: 4,102,310 vs 32,700).
    2026-03 부터 나아졌지만 지금도 20~25% 가 빈다.
  · 우리카드(아내) — **결제 이체가 61건 중 0건.** 매달 1.1~1.5M 을 긁는데
    그 돈이 나가는 기록이 없다.
  → 문제는 카드 마감일이 아니라 **수집 누락**이다. 미결제 추정의 57%가
    검증 불가능한 우리카드(아내)라, 지금 부채에 얹으면 부족한 부채를
    얹어놓고 고쳤다고 믿게 된다. 수집부터 메워야 한다.


## 1.11.23 — 2026-08-06

- 앱 `1.11.23` · Apps Script `변경 없음` (편집기 반영만 필요 · 웹앱 경로가 아님)
- 카드 부채 점검 — 안 나간 카드값이 얼마인지 재는 도구. **읽기만 합니다.**
  `부채` 시트에 카드 미결제액이 한 줄도 없어서, 긁은 날부터 결제일까지는
  순자산이 부풀어 보입니다(2026-08 기준 유동자산 11,368,220 · 유동부채 68,100,000).
  고치기 전에 ① 카드별 월 지출 ② 실제 카드대금 이체 ③ 둘의 대조를 먼저 잽니다 —
  앱의 `apiCardDue_` 가 쓰는 「전달 1일~말일」 모델이 진짜 마감일과 맞는지는
  숫자로만 알 수 있습니다. 안 맞으면 계좌 시트에 「마감일」 칸을 받아야 합니다.
- 이중집계는 없다는 것도 확인했습니다 — 카드 결제는 `이체`라 `CASH_G` 에서 빠집니다.


## 1.11.22 — 2026-08-06

- 앱 `1.11.22` · Apps Script `변경 없음`
- 카테고리 막대 자를 예산 하나로 통일 — 100% = 예산.
  예전엔 넘긴 줄만 자를 바꿔 달아서(막대 전체 = 지출액, 예산 지점에서 끊기)
  120% 인 줄의 보라가 83% 밖에 안 찼다. **더 많이 쓴 줄이 더 비어 보였다.**
  이제 넘긴 줄은 보라가 꽉 차고 넘긴 만큼(20%)이 오른쪽 끝에서 빨갛다.


## 1.11.21 — 2026-08-06

- 앱 `1.11.21` · Apps Script `43`
- 누가 썼나 선택 · 앞으로 나갈 돈 로딩 자리 확보
- 배포 43 = `inboxList` 가 J열(알림이 뜬 폰 주인)을 같이 실어보낸다.
  수신함에서 「누가 썼나」의 기본값이 로그인한 사람이 아니라 폰 주인이 되는 근거.


## 1.11.20 — 2026-08-06

- 앱 `1.11.20` · Apps Script `42`
- 쓴 사람: 집계·저장·수정·수신함이 F열을 먼저 본다


## 1.11.19 — 2026-08-06

- 앱 `1.11.19` · Apps Script `변경 없음`
- 입력자 열 비우기 — 수식을 다시 훑어 걸리면 멈추고, 백업 뒤 비운다


## 1.11.18 — 2026-08-06

- 앱 `1.11.18` · Apps Script `변경 없음`
- 입력자점검에 시트 수식 훑기 추가 — F열을 비워도 되는지 판정


## 1.11.17 — 2026-08-06

- 앱 `1.11.17` · Apps Script `변경 없음`
- 입력자 열 점검 — F열을 「쓴 사람」으로 재해석하면 뭐가 바뀌는지 먼저 잰다


## 1.11.16 — 2026-08-06

- 앱 `1.11.16` · Apps Script `41`
- 정리 제외 목록을 행 번호가 아니라 날짜·금액·이름으로


## 1.11.15 — 2026-08-06

- 앱 `1.11.15` · Apps Script `변경 없음`
- 중복 정리 적용을 확인창 달린 메뉴 항목으로


## 1.11.14 — 2026-08-06

- 앱 `1.11.14` · Apps Script `변경 없음`
- 이름 다른 이중집계를 1:1 로 짝지어 삭제 · 폴이 제외한 4행 기록


## 1.11.13 — 2026-08-06

- 앱 `1.11.13` · Apps Script `변경 없음`
- 합계 매칭(D) 폐기 · 이름 다른 이중집계 후보를 전부 보고


## 1.11.12 — 2026-08-06

- 앱 `1.11.12` · Apps Script `변경 없음`
- 중복 정리에 분할 합계·이름 불일치 규칙 추가 · 정리실행() 폐기


## 1.11.11 — 2026-08-06

- 앱 `1.11.11` · Apps Script `41`
- 결제 오탐 필터 · 카테고리 애칭을 서버에서


## 1.11.10 — 2026-08-06

- 앱 `1.11.10` · Apps Script `40`
- 공개 저장소에서 개인정보 분리


## 1.11.9 — 2026-08-06

- 앱 `1.11.9` · Apps Script `39`
- 맥박을 사람별로 · 백버튼으로 덮개 닫기 · #whobtn 널 가드


## 1.11.8 — 2026-08-05

- 앱 `1.11.8` · Apps Script `39`
- 버전 비교를 숫자로 — 서버가 더 낮을 때 「새 버전」이라 하던 것 · 알림 점을 제목 오른쪽으로


## 1.11.7 — 2026-08-05

- 앱 `1.11.7` · Apps Script `39`
- 내역 수정 오른쪽 위를 삭제 버튼으로 · 내역을 떠나면 「앞으로 나갈 돈」 접기


## 1.11.6 — 2026-08-05

- 앱 `1.11.6` · Apps Script `39`
- 확인 중일 때 화면 전체가 아니라 버튼만 돌게


## 1.11.5 — 2026-08-05

- 앱 `1.11.5` · Apps Script `39`
- 설정에 「점검」 묶음 — 알림 연결 + 앱 버전을 자동 점검(앱 실행·1시간) · 필터 판정 한 곳으로 · 죽은 CSS 정리


## 1.11.4 — 2026-08-05

- 앱 `1.11.4` · Apps Script `39`
- 홈 문구 덜어내기(페이스 경고 문단 · 「N월 N일까지」 중복) · 설정 「새로고침」을 「버전 확인 + 업데이트 확인」으로


## 1.11.3 — 2026-08-05

- 앱 `1.11.3` · Apps Script `39`
- 이체 포함 토글을 목록 머리줄로 · 탭 재탭/필터 이동 시 맨 위로 · 뒤로가기로 입력창 닫기


## 1.11.2 — 2026-08-05

- 앱 `1.11.2` · Apps Script `39`
- 폰 알림 끊김 감지(맥박·수신로그) · 유형/사람 필터 · 자본거래 숨김 · 알림 연결 확인 복구


## 1.11.1 — 2026-08-05

- 앱 `1.11.1` · Apps Script `38`
- 고정비 무시 기능 · 금액+결제수단 대조 · 저장 실패 표시 · 다음 달 카드 제외


## 1.11.0 — 2026-08-05

- 앱 `1.11.0` · Apps Script `37`
- 앞으로 나갈 돈을 내역 탭으로 · 고정비 등록 · 카테고리 클릭 필터


## 1.10.1 — 2026-08-05

- 앱 `1.10.1` · Apps Script `36`
- 토스 모임통장 카드가 폴 개인계좌로 잡히던 것 수정


## 1.10.0 — 2026-08-05

- 앱 `1.10.0` · Apps Script `35`
- 수신함에 소유자(w) 구분 추가 · 다가오는 카드 결제 CSS 누락 수정


## 1.9.0 — 2026-08-05

- 앱 `1.9.0` · Apps Script `34`
- 로그인 화면을 브랜드·버튼·도움말 세 덩어리로. 마지막에 들어온 사람을 기억해
  이름을 부르고, 막혔을 때 무엇을 해야 하는지 접힌 도움말로 안내합니다.
- 브랜드 마크 교체(집 지붕 + 장부 두 줄). 옛 가로 3줄 마크는 런처와 헤더에서
  햄버거 메뉴로 읽혔습니다. 앱 아이콘 4종도 새 마크로 다시 만들었습니다.


## 1.8.0 — 2026-08-05

- 앱 `1.8.0` · Apps Script `34`
- 내역 필터를 낮은 시트로. 칩에 금액·건수를 얹고, 고르는 즉시 뒤 목록이 걸러집니다.
  결제수단은 소유자(공동/폴/아내)로 묶어 그룹째 선택. 검색은 전체 화면으로 옮기고
  합계·건수·월평균과 최근 검색어, 키워드 하이라이트를 붙였습니다.


## 1.7.0 — 2026-08-05

- 앱 `1.7.0` · Apps Script `34`
- 페이스 차트 경과일 축 · 앞으로 나갈 돈에 남은 고정지출 합산


## 1.6.1 — 2026-08-05

- 앱 `1.6.1` · Apps Script `변경 없음`
- 수입이 거의 없을 때 손익 배지가 무의미한 배수를 보여주던 문제


## 1.6.0 — 2026-08-05

- 앱 `1.6.0` · Apps Script `변경 없음`
- TURN 9 잠금 화면 · TURN 8 홈 히어로 + 앞으로 나갈 돈 · TURN 3d 설정 정리


## 1.5.0 — 2026-08-05

- 앱 `1.5.0` · Apps Script `33`
- 여기서부터 시맨틱 버전. 이전에는 파일마다 따로 도는 정수를 썼습니다
  (`app.js?v=22` · `app.css?v=13` · `sw.js hb-v2-20` · `APP_V v22`).
- 담긴 내용은 아래 `v22` 까지와 같습니다. 기능 변화 없이 번호 체계만 바꿨습니다.

---

## 이전 (정수 시절)

| 앱 | Apps Script | 내용 |
|---|---|---|
| `v22` | 33 | 내역(`tx2`) 응답을 **사전 + 번호**로 압축. 7월분 40.8KB → 15.0KB (−63%). 앱은 `txExpand()` 로 펴서 쓰므로 화면 코드는 그대로. 옛 형식 캐시도 계속 동작 |
| `v21` | 32 | 리포트 첫 진입 시 **PIN 설정 안내**(거절하면 다시 안 물음). 설정에 **알림 연결 확인** — 결제수단 소유자로 폰을 갈라 마지막 수신 시각 표시 |
| `v20` | 31 | **주간 예산 대비**(홈 카테고리 카드에 주/월 토글, 주 예산 = 월 예산 × 그 주가 이 달에 걸친 날 수 ÷ 그 달 일수). **다가오는 카드 결제**(계좌 시트 H 결제일 · I 출금계좌). 예산 초과분만 빨갛게 |
| `v19` | — | 낭비 표시 제거(시트 칸과 API 는 남김). 홈 카테고리 전부 표시. 입력 화면 칩 높이 수정. **리포트 PIN 잠금** |
| `v18` | — | 건전성 막대를 실제 값으로 그리고 초과/부족분만 빨갛게. 부채비율을 거꾸로 그리던 꼼수 제거 |
| `v17` | — | 저장을 기다리지 않고 화면 먼저 반영. 입력 중 다른 앱 갔다 와도 내용 유지 |

그 이전 이력은 `claude/가계부앱_인계문서.md` 를 보세요.

---

## 배포 순서 (잊지 말 것)

1. `python3 bump.py <버전> "<설명>"` — Apps Script 도 같이 바꿨으면 배포 뒤에 `--gas <번호>` 로 다시 적기
2. `app.js` `app.css` `index.html` `sw.js` 를 GitHub `main` 에 커밋 → Pages 자동 배포
3. Apps Script 를 고쳤으면: 편집기에 붙여넣기 → **저장(디스크 아이콘)** → 배포 ▾ → 배포 관리 → 연필 →
   버전 드롭다운에서 **새 버전** → 배포
4. 시트 스키마나 소유자 매핑을 건드렸으면 **스크립트 속성 `API_VER` 를 +1** — 안 그러면 캐시(600초/1500초) 때문에 안 보입니다
5. 실제 앱에서 눈으로 확인
