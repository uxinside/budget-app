# 우리집 가계부 (budget-app) — Phase 3 인수인계 v2

작성: 2026-08-04 / v1(같은 날 작성) 이후 세션 결과 반영
**v1을 대체합니다.** v1의 진단 중 3건이 실측으로 틀린 것이 확인됐습니다.

---

## 0. 30초 요약

- Apps Script 백엔드를 **`gas/` 아래로 편입 완료**. 이제 저장소 하나에 프런트+백엔드가 다 있습니다.
- **v1의 오진 3건 정정**: 사람 매핑 테이블은 이미 있고(백필 불필요), 엔드포인트 통합은 서버에 이미 구현돼 있고, 중복·가족계좌 정리는 **이전 세션에서 이미 실행됐습니다.**
- 그 결과 **v1의 검증 기준 "8월 2건 / 78,500"은 폐기**해야 합니다. 실측은 **13건 / 164,600**이고 이건 정상입니다.
- 대신 **새 중복 패턴 2개**를 찾았습니다 (금액 분할형 이중집계, 가맹점명 불일치형).
- 남은 큰 일은 **성능(로그인 지연)** 과 **UI(전환 버튼 제거 + 설정 화면)** 입니다.

---

## 1. 시스템 구성

| 레이어 | 위치 |
|---|---|
| 프런트엔드 (정적 PWA) | GitHub `uxinside/budget-app` |
| 배포 | https://uxinside.github.io/budget-app/ |
| 백엔드 API | Apps Script 웹앱 `/exec` |
| Apps Script 프로젝트 | `1Xfo6fLRK0R0FDhhynrs-qJzMbkhbJinju26n_wNgFkYlryJDa_faigss` ("우리집 통합 가계부 - 입력앱") |
| 데이터 | Google Sheets `1Hc7wfvucANXFZp9d1i9X26oS2gUc9Mofg9lrzGWScWU` ("우리집 통합 가계부") |
| 인증 | GIS ID 토큰 → 서버에서 `tokeninfo` 검증 → 이메일 화이트리스트 |

**허용 계정** (`gas/api.js:8`)
`uxinside@gmail.com` → `폴` / `lovelykoni33@gmail.com` → `아내`

**거래내역 시트 컬럼**
`A날짜 B구분 C대분류 D내용 E결제수단 F입력자 G금액 H고정/변동 I메모 J낭비 K사용처`

### 커밋 이력

| 커밋 | 내용 | 상태 |
|---|---|---|
| `14142fe` | API 재시도·타임아웃 (v1의 중단 지점) | pushed |
| `1497fd1` | **Apps Script 백엔드를 `gas/` 로 편입** (clasp clone, 10파일) + `.gitignore` | pushed |
| `60093e2` | **`gas/cleanup.js` 추가** — 정리 dry-run | pushed |
| `c3ba65d` | `gas/.clasp.json` rootDir `""` → `"."` | **로컬만. `git push` 필요** |

---

## 2. v1 오진 정정 — 여기가 제일 중요합니다

### 오진 ① "사람 매핑 테이블 신설 + 기존 데이터 백필" → **둘 다 불필요**

매핑 테이블은 **`계좌` 시트 D열(소유자)** 로 이미 존재합니다.
`gas/app2.js:5 accounts_()` 가 읽고 → `gas/api.js:70` 에서 `own[결제수단] = 소유자` 맵 생성.

결정적인 줄 — `gas/api.js:267`:

```js
var owner = agg.own[pay] || '공동';
```

`who`는 **시트에 저장된 값이 아니라 조회 시점에 결제수단에서 계산**됩니다.
시트 F열(입력자)에 사람이 들어 있어도 API는 쓰지 않습니다.

**→ 백필이 원천적으로 불필요합니다.** `계좌` 시트 D열만 채우면 과거 전체가 즉시 갈립니다.
전 건이 `공동`으로 보이는 진짜 원인은 **`계좌` 시트 D열이 안 채워져 있어서**입니다.

계좌 이름에 이미 소유자가 드러나 있어 작업이 쉽습니다:

| 계좌 (실측) | 추정 소유자 |
|---|---|
| 토스부부 | 공동 |
| 토스개인 · 하나은행 · 카카오뱅크 · 네이버페이 · 카카오페이 | 폴 |
| 토스뱅크(아내) · 카카오 체크카드(아내) · 카카오뱅크(아내) | 아내 |

> 네이버페이/카카오페이를 폴·아내로 나누려면 `계좌` 시트에 `네이버페이(아내)` 같은 행을 추가하고
> 임포트 시 그 이름으로 들어오게 해야 합니다. 현재는 단일 이름입니다.

### 오진 ② "엔드포인트 통합(`boot3`)을 새로 만들어야" → **서버에 이미 있습니다**

`gas/api.js:401`:

```js
if (api === 'init') return { ok:true, me:…, data:{ boot: apiBootC_(), month: apiMonthC_(p.ym) } };
```

`init`이 boot+month를 **캐시 버전으로** 한 번에 반환합니다.
`app.js`의 `start()`가 `boot2` → `month` 2회 직렬로 부르는 것을 `init` 1회로 바꾸면 끝. **서버 작업 0.**

**추가 서버 버그** — `gas/api.js:399` 에서 `boot2`가 캐시 없는 `apiBoot_()`를 호출합니다.
바로 옆에 `apiBootC_()`(25분 캐시)가 있는데 안 씁니다. 부팅마다 거래내역 전체 스캔 + 시트 4개 재독.
**로그인 지연의 서버측 주범.** 한 단어 수정.

### 오진 ③ "중복·가족계좌 정리를 새로 짜야" → **이전 세션에서 이미 실행됨**

`gas/diag.js` 의 `정리실행()` 이 이미 돌았습니다. 2026-08 실측으로 확인:

- 판교매일식당·헤이븐커피 **없음** (가족계좌 2건 이미 삭제됨)
- 카페 스테이인터뷰르·오근장막국수의 네이버페이 중복본 **없음**

**v1의 검증 기준 "8월 2건 / 78,500"은 폐기하십시오.** 그건 정리 *전* 스냅샷입니다.
현재 8월은 **13건 / 164,600** 이고 정상입니다 (8/2~8/3 신규 유입 포함).

---

## 3. 현재 데이터 실측 (2026-08-04 기준)

### `정리점검()` 결과

```
삭제 예정 4행 │ A 제외규칙 0행 │ B 분할결제 2그룹 │ C 이중집계 2행 │ ±1일 후보 0행

2026-08  13건   164,600  →  13건   164,600   (변화 없음)
2026-07  233건 7,666,164 → 232건 7,592,534   (-73,630)
2026-06  194건 7,642,367 → 194건 7,642,367
2026-05  262건 7,780,425 → 262건 7,780,425
2026-04  222건 9,406,048 → 222건 9,406,048
2026-03  215건 9,885,536 → 215건 9,885,536
```

삭제 후보 4행 (전부 과거 건, 8월 아님):

| 행 | 날짜 | 내용 | 수단 | 금액 | 판정 |
|---|---|---|---|---:|---|
| 2021 | 01-13 | 주식회사 카카오 | 카카오페이 | 29,000 | C 이중집계 → #2022 토스부부 유지 |
| 2090 | 01-21 | 주식회사 카카오 | 카카오페이 | 6,500 | B 병합 → #2086 (35,500) |
| 2396 | 02-18 | 주식회사 카카오 | 카카오페이 | 13,900 | B 병합 → #2395 (46,800) |
| 4078 | 07-15 | (주)교보문고 | 카카오페이 | 73,630 | C 이중집계 → #4079 토스부부 유지 |

실효는 **7월 −73,630, 건수 −4**뿐. B 병합은 그룹 합계를 유지해 총액 불변.

### 2026-08 지출 13건 전체 (합 164,600)

```
8/1  외식/배달  카페 스테이인터뷰르 투어   토스부부           39,500
8/1  문화/여가  나나랜드                  토스부부            1,700
8/1  문화/여가  나나랜드                  토스부부            2,400
8/1  문화/여가  나나랜드                  네이버페이          4,100
8/1  외식/배달  오근장막국수              토스부부           39,000
8/1  식비       (주)화랑마트              토스부부            6,100
8/1  문화/여가  나나랜드                  카카오 체크카드(아내) 2,000
8/2  기타지출   컬리(멤버스)              네이버페이          1,900
8/2  통신비     나이스정보통신            토스부부           20,000
8/2  기타지출   나이스_카드(마스터)        네이버페이         20,000
8/2  식비       쿠팡(쿠페이)              토스부부           22,900
8/2  기타지출   발급수수료납부            하나은행            1,000
8/2  식비       바나프레소                하나은행            4,000
```

---

## 4. 새로 발견한 문제

### ① 금액 분할형 이중집계 — 현재 로직이 못 잡습니다

```
8/1  나나랜드  토스부부    1,700
8/1  나나랜드  토스부부    2,400
8/1  나나랜드  네이버페이  4,100   ← 1,700 + 2,400 = 4,100
```

간편결제 원장 1건이 계좌 원장 2건으로 쪼개져 들어왔습니다.
`cleanup.js`의 `B)`는 같은 결제수단끼리만 묶고 `C)`는 금액 완전일치를 요구해 둘 다 놓칩니다.
**4,100이 중복이라 빠져야 합니다.**

필요한 규칙: *같은 날 · 같은 가맹점 · (간편결제 1건의 금액) == (계좌 N건의 합)* → 간편결제 쪽 삭제.

### ② 가맹점명 불일치형 이중집계

```
8/2  나이스정보통신      토스부부    20,000  (통신비)
8/2  나이스_카드(마스터)  네이버페이  20,000  (기타지출)
```

같은 날 같은 금액인데 `cl_nk_()` 정규화 결과가 달라 안 잡힙니다.

### ③ 카테고리 오분류

`나이스정보통신`(결제대행사 NICE)이 **통신비**로 분류돼 있습니다.
`gas/import.js`의 `DEFAULT_RULES`에서 통신 관련 규칙이 `["정보통신","기타지출",""]` 보다 앞에 있어서
먼저 걸립니다. 규칙 순서를 조정해야 합니다.

### ④ 코드 버그 목록

| # | 위치 | 내용 | 심각도 |
|---|---|---|---|
| 1 | `gas/api.js:399` | `boot2`가 캐시 없는 `apiBoot_()` 호출 (오진 ② 참조) | **높음** |
| 2 | `gas/api.js:249,371` | `apiTx_`는 월별 `min~max` 행 범위만 읽는데 `apiAdd_`는 `appendRow`. **앱으로 과거 날짜를 입력하면 조회 범위 밖으로 떨어져 안 보입니다** | **높음** |
| 3 | `app.js:975` | `$('#whobtn').onclick` — HTML에서 버튼만 지우면 `null.onclick` TypeError로 **이후 모든 바인딩이 죽습니다** | **높음** |
| 4 | `gas/api.js:367` ↔ `gas/import.js:143` | `apiUpdate_` memo가 I열(9)인데 임포트가 같은 I열에 `'임포트'` 마커를 씀 → 앱에서 수정하면 `임포트취소`가 깨짐 | 중간 |
| 5 | `gas/import.js:126,165` | 중복키 `날짜+내용8자+금액` 에 **±1일 허용 없음**. 승인일/출금일이 어긋나면 못 잡음 | 중간 |
| 6 | `sw.js:3` | 셸 프리캐시가 `'./app.js'`(쿼리 없음)인데 페이지는 `./app.js?v=4` 요청 → 별개 키. `V`(`hb-v2-2`)를 올리지 않으면 구버전 잔존 | 중간 |
| 7 | `gas/Code.js:9` | `?api=` 없이 `/exec` 호출 시 44KB 구버전 웹앱 UI(`Index.html`) 렌더. 살아있는 레거시 경로 | 낮음 |

### ⑤ 404 원인 — 후보 좁혀짐

- `gas/appsscript.json` 은 `executeAs: USER_DEPLOYING`, `access: ANYONE_ANONYMOUS`.
  **배포 설정은 정상입니다. 이 후보는 지워도 됩니다.**
- **남은 유력 후보 2개**
  1. **다중 구글 계정 `/u/1/` 리다이렉트** — 편집기 URL이 `script.google.com/u/1/...` 로 확인됐습니다.
     브라우저에 계정이 여럿이고 `uxinside`가 두 번째입니다. v1이 꼽은 후보 ②와 정확히 일치.
  2. **서비스워커** — 위 버그 6번. 오프라인 폴백이 `index.html`을 반환하므로 셸 요청이 엉뚱하게 처리될 수 있음.

---

## 5. app.js 구조 (1026줄, ES5 IIFE, 빌드 없음)

| 블록 | 줄 | 핵심 |
|---|---|---|
| 유틸 | 10–45 | `$`, `el`, `esc`, `C`(금액), `SG`, 날짜 헬퍼 |
| 카테고리 | 47–70 | `CATMAP` 22종 → 배지 2글자 + hue |
| 상태 `ST` | 72–80 | `token/me/boot/month/tx/ym/tab/f/form` |
| 인증 | 82–140 | GIS, `sessionStorage['idt']` |
| API | 142–176 | `api(name, params, _try)` — 25s 타임아웃 + 3회 재시도 |
| 부팅 | 178–224 | `start` → `loadMonth` → `loadTx` → `refreshAll` |
| 홈 | 281–462 | 손익 / 페이스(인라인 SVG) / 카테고리 / 사람 4카드 |
| 내역 | 464–678 | 필터칩, 일자 그룹, 롱프레스 낭비토글, 픽커 시트 |
| 입력·수정 | 680–927 | 풀스크린 모달 + 숫자 키패드 |

**주의할 사실**

- **스켈레톤 UI는 이미 있습니다** (243–256). v1의 요청1-F는 할 일이 아닙니다.
- **"사람" 칩은 하드코딩** — `app.js:571` 이 `['폴','아내','공동']`.
  **아내는 이미 목록에 있습니다.** 눌러도 0건인 건 데이터 문제(오진 ① 참조).
- `ST.f.who` 는 **단일값**(78행), `passFilter` 는 `r.who !== f.who` 엄격 일치(488행).
  다중 선택·공동 합산을 하려면 배열화 필요.
- `paintWho()` 는 `#whonm` 에 항상 `'가구 전체'` 를 넣습니다(229행). 헤더 이름은 이미 무의미.
- **`report` / `settings` 탭은 스텁**입니다 (`renderSoon()`, 929–938).
- **로그아웃·새로고침 진입점이 `#whobtn` 뿐**입니다 (976–985 `sheet('계정', …)`).
  → **전환 버튼 제거는 설정 화면 구현이 전제입니다.**
- `visibilitychange` 마다 `refreshAll()` (1019–1024) → 복귀할 때마다 `month`+`tx2` 2콜.
  홈만 보고 있어도 `tx2`를 부릅니다.
- `ST.me` 는 헤더 말고도 `payList()`(694, 결제수단 개인/공동 분류)와 입력 모달 아바타(767)에서 사용.
  버튼을 지워도 `ST.me` 와 `accounts[].owner` 는 살려야 합니다.

---

## 6. gas/ 백엔드 구조

| 파일 | 내용 |
|---|---|
| `api.js` | JSON API v2 — `boot2 month tx2 report2 init add2 upd del waste ping2`. 토큰 검증, `txAgg_` 월별 집계 캐시, 예산, 응답 캐시 |
| `Code.js` | `doGet` 라우터 + 레거시 API(`boot overview add undo master summary`) + `Index.html` 템플릿 |
| `app2.js` | **`accounts_()` 계좌·소유자 매핑** (사람 필터의 핵심), `listTx`, `summary2_`, `burndown_` |
| `import.js` | 임포트 도구 + `DEFAULT_RULES` 분류규칙 ~170개 + 중복 스킵 + `임포트취소` |
| `cleanup.js` | **이번에 추가.** `정리점검()` dry-run / `정리적용()` |
| `diag.js` | 구버전 `정리실행()` — **이미 실행됨. `cleanup.js`로 대체됐으니 삭제 권장** |
| `bs.js` `graphs.js` `phase0.js` | 재무상태표 / 그래프 / 초기 세팅 |
| `Index.html` | 44KB 레거시 웹앱 UI |
| `appsscript.json` | 타임존 Asia/Seoul, V8, 웹앱 설정 (정상) |

---

## 7. 열린 결정사항

### 결정 필요 ①: `정리적용()` 을 언제 돌릴 것인가

- **A. 규칙 보강 후 한 번에** (직전 세션 권장안) — §4의 ①②를 `cleanup.js`에 넣고 다시 점검 → 적용.
  백업 시트가 한 번만 생기고, 놓친 중복까지 같이 잡습니다.
- **B. 지금 4행만 적용** — 7월 −73,630 확보하고 다음 단계로. ①②는 나중에.
- **C. 요청 3을 여기서 종료** — 8월이 이미 정상이므로 데이터 정리는 충분하다고 보고 다음으로.

> **⚠️ 폴이 다른 창에서 별도 방안을 제안받았다고 하셨습니다.
> 그 내용은 이 문서에 반영돼 있지 않습니다. 아래 작업 순서보다 그쪽을 우선 검토하십시오.**

### 결정 필요 ②: 가족계좌 제외를 규칙으로 승격할지

현재 `cleanup.js`의 `CL_EXCLUDE` 는 날짜+상호 하드코딩이라 다음 임포트 때 되살아납니다.
`계좌` 시트에 "집계제외" 플래그 컬럼을 두고 `txAgg_` 에서 거르는 편이 재발 방지에 낫습니다.

---

## 8. 수정된 작업 순서

v1의 8단계를 실측 기준으로 조정한 것입니다.

| # | 작업 | 비고 | 크기 |
|---|---|---|---|
| 1 | **`계좌` 시트 D열 소유자 채우기** | 코드 변경·백필 불필요. §2 오진① 표 참고 | 소 |
| 2 | **`gas/api.js:399` `apiBoot_()` → `apiBootC_()`** | 로그인 지연 주범. 한 단어 | 소 |
| 3 | **`app.js start()` 를 `init` 1콜로 전환** | 라운드트립 2→1. 서버 작업 0 | 중 |
| 4 | **타임아웃 25s→8s, 재시도 3→2** (`app.js:155,158`) | 실패가 빨리 드러남 | 소 |
| 5 | **`sessionStorage` 캐시 + stale-while-revalidate** | 탭 전환 체감 | 중 |
| 6 | **사람 필터**: `ST.f.who` 배열화 + 다중선택 + "폴 = 폴+공동" 규칙 | 아내는 이미 목록에 있음 | 소 |
| 7 | **설정 화면 구현** (계정 표시 / 로그아웃 / 새로고침) | 8번의 전제 | 대 |
| 8 | **전환 버튼 제거** — `index.html` 37–41, `app.css` `.who*`, **`app.js:975` 핸들러 반드시 같이 삭제**, 헤더 중앙정렬 재조정 | §4 버그 3 주의 | 중 |
| 9 | **중복 규칙 보강** — §4 ①② 를 `cleanup.js`·`import.js` 에 반영 | 결정 ① 에 따름 | 중 |
| 10 | **캐시 버스팅** — `app.js?v=5`, `app.css?v=4`, **`sw.js` 의 `V` 도 반드시 올릴 것** | §4 버그 6 | 소 |
| 11 | 404 재현 시 `/u/1/` 다중계정 가설 검증 | §4 ⑤ | 중 |

### 새 검증 기준 (v1 것은 폐기)

- 로그인부터 대시보드 표시까지 **3초 이내**
- "사람" 필터에서 폴/아내/공동을 눌렀을 때 **결과 건수가 실제로 달라진다**
- 헤더에 전환 버튼이 없고 월 네비게이션이 중앙 정렬
- 설정 화면에서 로그아웃이 된다
- 앱으로 **과거 날짜** 거래를 입력해도 내역에 보인다 (§4 버그 2)

---

## 9. 환경 제약 — 다음 창에서 꼭 알아야 할 것

- **Cowork 클라우드 세션의 셸(`device_bash`)은 네트워크가 막혀 있습니다.**
  `git ls-remote` → `HTTP 403 from proxy`. `git push` · `clasp push` · `npm i` 는 사용자가 직접 터미널에서.
  읽기·쓰기·커밋·`node --check` 는 에이전트가 직접 가능합니다.
- **마운트가 파일 삭제를 막습니다.** git이 `.git/index.lock` · `HEAD.lock` 을 못 지워
  커밋이 `cannot lock ref 'HEAD'` 로 실패할 수 있습니다. 커밋 전에 lock 파일을 `.git/_stale/` 로 옮기면 됩니다.
  잔여물 정리: 폴 터미널에서 `git gc --prune=now`.
- **크롬 제어는 됩니다.** Apps Script 편집기에서 함수 실행, 시트 판독 모두 에이전트가 직접 했습니다.
  시트 데이터 판독에는 gviz 엔드포인트가 편합니다:
  `https://docs.google.com/spreadsheets/d/<ID>/gviz/tq?tqx=out:html&sheet=<시트명>&tq=<쿼리>`
- **clasp 3.3.0 은 파일을 올리고도 `Script is already up to date.` 를 찍습니다.**
  실패로 오해하지 마십시오. 확인은 편집기 파일 목록으로.
- `clasp push` 는 반드시 **`gas/` 안에서** 실행 (`.clasp.json` 위치).
- **`~/.clasprc.json` 은 절대 커밋 금지** (`.gitignore` 에 등록해 뒀습니다).

---

## 10. 새 창 시작 절차

1. 데스크톱 앱에서 새 Cowork 태스크
2. 폴더 `budget-app` 연결 (또는 저장소 `uxinside/budget-app` 연결)
3. **이 문서를 첨부**
4. 프롬프트:

```
budget-app Phase 3 이어서 한다. 첨부한 인수인계 v2를 먼저 읽어줘.
v1은 폐기됐고 v2가 최신이다.

먼저 확인할 것:
- git push 안 된 로컬 커밋 c3ba65d 가 있다
- §7 결정사항 두 개가 열려 있다
- 다른 창에서 받은 대안이 있으면 §8 작업 순서보다 그쪽을 먼저 검토한다

§8 작업 순서 1번(계좌 시트 D열)부터 시작하되, 각 단계마다
변경 내용을 보여주고 확인받은 뒤 커밋해줘.
```

---

## 11. 주의

- **동시에 두 창에서 작업하지 마십시오.** `clasp push` 는 원격 파일을 통째로 덮어쓰고,
  git 은 서로 다른 커밋을 만듭니다. 한쪽만 진행할 것.
- 작업 중 결정사항은 계속 이 문서에 반영해 파일로 남기십시오. 창이 죽어도 복구됩니다.
